# M10 — Port-explicit policies, presets, and observability

Harden the network policy model by making ports explicit per rule, replace the `Policy::unrestricted()` escape hatch with a CLI-local preset system, and expose a unified event surface across every policy-enforcing layer. Builds on the L3 flow landed in M9-S18..S20 and amends three specific decisions from that work (L3 destination_port predicate; VM-egress reject → `sandbox_dnat` conditional DNAT; Envoy access_log format). One release, one migration event (V004 SQLite migration; running sessions stop and restart at upgrade).

**Spec references:**

- `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — primary spec; M10-S1..S6.
- `.tasks/specs/2026-04-19-l3-envoy-mitmproxy-flow-design.md` — receives amendment #1 in M10-S1 (destination_port on L3), #2 in M10-S3 (sandbox_policy VM-egress → sandbox_dnat), #3 in M10-S2 (access_log JSON format); spec-text edits for #1 and #2 land in M10-S6.

---

## M10-S1 — v2 port-explicit policy schema, compiler rewrite, and DB migration

**Entry criteria.** M9-S20 complete.

**Spec reference.** `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — Part 1 in full; "Amendments to the L3 spec and its landed implementation" § 1; "Cleanup sites → Bare-`*` removal"; "Known gaps" bullet on Envoy listener `use_original_dst` empirical check.

**Rationale.** Lands the foundational rule-model change. Every downstream M10 session depends on v2 rules: S3's deny-logger uses the `policy_allow_{tcp,udp}` concat sets emitted here; S5's presets emit v2 rules; event shapes reference explicit port. Keeping schema, compilation across three back-ends, migration, and bare-`*` removal in one session avoids a broken intermediate where rule shape and compiler disagree. The L3 destination_port amendment lands here because it's a compiler change touching the same files as the port-explicit rework.

**In scope.**

- Part 1 § "Rule shape" — rule identity `(host, port)`; new/tightened validators; removals (bare-`*`, `protocol: any/http/https`, `Policy::unrestricted()`).
- Part 1 § "Compiler consequences → nftables" — per-protocol concat sets `policy_allow_tcp` / `policy_allow_udp` keyed on `ipv4_addr . inet_service`; removal of hardcoded `dport { 80, 443 }`; DNS→policy `(ip, port)` join in sandboxd.
- Part 1 § "Compiler consequences → Envoy" — `destination_port` predicate on L1/L2/L3 `FilterChainMatch` (this is L3 amendment #1); deletion of bare-`*` `default_filter_chain` and L1 catch-all compiler branches.
- Part 1 § "Compiler consequences → mitmproxy" — `PolicyCompiler::compile_mitmproxy` emits port; addon reads `flow.request.port`.
- Part 1 § "Compiler consequences → Path matcher semantics" — per-segment glob with `**` / `*` / `?`; replacement of `fnmatch.fnmatchcase` in the addon.
- Part 1 § "Schema bump and migration" — v1 parse hard-reject with prescribed error text; SQLite migration V004 (tighten `protocol` CHECK to `tcp/udp`; add `port` column with `CHECK (port BETWEEN 1 AND 65535)`; delete v1-shaped rows via table-copy). `policy_reset_on_upgrade` emitted as a tracing event here; re-wired to the ring buffer in S2.
- "Cleanup sites → Bare-`*` removal" — all eight call sites in the spec's explicit list.
- Known-gaps item: empirical check that `destination_port` on `FilterChainMatch` works with the pinned Envoy version's `original_dst` listener filter — first-step verification before building out the chain compiler, mirroring the M9-S19 tunneling-config pattern.

**Explicitly deferred.**

- Deny-logger component + nftables `sandbox_policy` → `sandbox_dnat` restructure (L3 amendment #2) → M10-S3.
- Event ingestion pipeline in sandboxd → M10-S2.
- Orphaned daemon-side preset helpers `preset_allow_github` / `preset_allow_npm` → M10-S5 (deleted in the same pass that lands their CLI replacements, so nothing disappears before its substitute exists).
- L3 spec text edit for amendment #1 → M10-S6.

**Exit criteria.**

- `cd sandboxd && cargo nextest run --workspace` green; `make test-integration` green.
- `make test-e2e` full suite green; partial runs do not count.
- New unit coverage: `(host, port)` uniqueness failure surfaces with the spec's error-shape (names every source); path matcher golden tests for `*`, `**`, `?`, literals.
- V004 migration test: a seed DB containing v1-shaped rows lands cleanly, emits `policy_reset_on_upgrade` per affected session (tracing), and leaves those sessions with no attached policy.
- Grep sweep: zero matches for `Policy::unrestricted`, `is_unrestricted`, `--unrestricted` (CLI flag), bare-`*` host literals in compiler arms, `dport { 80, 443 }`, `fnmatch.fnmatchcase`.
- `/review` (or `kio-reviewer`) run over the full diff; every finding addressed in-branch.
- `make docs-build` green; `starlight-links-validator` clean.

---

## M10-S2 — Event ingestion pipeline, structured producer emission, and lifecycle events

**Entry criteria.** M10-S1 complete.

**Spec reference.** `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — Part 3 §§ "Event surface", "Event shape", "Event categories", "Envoy access log", "CoreDNS structured emission", "mitmproxy structured emission", "Retention" (ring-buffer subsection); "Amendments to the L3 spec and its landed implementation" § 3.

**Rationale.** Lands the observability backbone. Per-session ring buffer, `vm_ip → session-id` in-memory map, per-session host-directory bind mount replacing the gateway `/var/log` tmpfs, inotify watcher in sandboxd, and three producer format changes — Envoy access_log as JSON across L1/L2/L3 (amendment #3), CoreDNS JSONL, mitmproxy JSONL. Lifecycle events begin emitting from sandboxd itself. Landing before the deny-logger (S3) means S3's new component plugs into a live pipeline instead of writing to stderr during an interim.

**In scope.**

- Part 3 § "Event shape" — common envelope; RFC 3339 millisecond timestamps; sandboxd stamps `session` via the new `vm_ip → session-id` map derived from per-session `NetworkInfo.vm_ip` in `sandbox-core/src/network.rs`; map updated on session create / stop.
- Part 3 § "Event categories → Traffic events" rows for `envoy`, `dns`, `mitmproxy` only (the `deny-logger` row is S3's responsibility).
- Part 3 § "Event categories → Lifecycle events" — the full table: `gateway_booting`, `gateway_ready`, `policy_applied` / `policy_updated` (the latter two accept `source_presets` from the CLI; CLI population lands in S5 but the daemon accepts and records the field now), `policy_reset_on_upgrade` re-wired from S1's tracing path, `health_degraded` / `health_restored`, `gateway_shutdown`.
- Part 3 § "Envoy access log" — flip `text_format_source.inline_string` to `json_format_options` across L1/L2/L3; harmonized field map (5-tuple, matched filter chain, cluster, response flags, start time, duration; L3-specific CONNECT authority remains L3-only); `tests/e2e/test_m4_policy.py` assertions rewritten to JSON parsing.
- Part 3 § "CoreDNS structured emission" — second output path as JSONL in the per-session bind-mount dir; existing `log.Infof` preserved; existing `/etc/coredns/resolved.json` IP-report stream unchanged.
- Part 3 § "mitmproxy structured emission" — second output path via the same JSONL mechanism.
- Part 3 § "Retention → in-memory ring buffer" — per-session ring buffer; size as a config knob; replay-on-connect semantics.
- Gateway container infra — replace `/var/log` tmpfs with a per-session host-dir bind mount (template: listener-dir bind at `gateway.rs:241-252`); sandboxd-side inotify watcher + JSONL line reader parallel to the M9-S18 listener-file rename mechanism.
- API DTO boundary — event serialization goes through explicit DTOs, not flattened domain structs.

**Explicitly deferred.**

- Deny-logger component + nftables restructure → M10-S3.
- `GET /sessions/{id}/events` HTTP endpoint, `sandbox events` CLI, persistent sink → M10-S4.
- Default numeric values for `events.ring_buffer_size`, rate caps, connection caps, retention days → M10-S6 (measurement-driven).

**Exit criteria.**

- cargo nextest green; `make test-integration` green; `make test-e2e` full suite green.
- New integration test: apply a policy, trigger one Envoy L3 flow + one CoreDNS resolution + one mitmproxy HTTP request; all three events appear in the per-session ring buffer with the `session` field stamped by the `vm_ip` map and the envelope fields present.
- Lifecycle events visible in the ring buffer across a `start → apply-policy → stop` cycle, including `policy_reset_on_upgrade` when V004 re-runs against a seed DB.
- `tests/e2e/test_m4_policy.py` assertions pass against JSON access_log records.
- `/review` pass clean; `make docs-build` green.

---

## M10-S3 — Deny-logger component and nftables DNAT restructure

**Entry criteria.** M10-S2 complete.

**Spec reference.** `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — Part 3 §§ "Deny-logger component" (Placement in the nftables pipeline, Listener design, Hardening rules, Liveness posture); "Amendments to the L3 spec and its landed implementation" § 2.

**Rationale.** Closes the silent-denial gap acknowledged by the L3 spec. Today the VM-egress reject in `sandbox_policy` produces ICMP-admin-prohibited or TCP RST with no structured signal. This session replaces that reject with conditional DNAT in `sandbox_dnat` — allow → Envoy, TCP deny → deny-logger `:10001`, UDP deny → deny-logger `:10002` — using the `policy_allow_{tcp,udp}` concat sets from S1, and introduces a new gateway component whose sole job is structured logging of the deny. The `deny-logger` row in Part 3's traffic-event table is filled in here; events flow into the bind-mount JSONL already consumed by sandboxd from S2. Envoy-egress rules in `sandbox_policy` are preserved.

**In scope.**

- Part 3 § "Placement in the nftables pipeline" — cross-table restructure: `sandbox_dnat` absorbs VM-egress filtering via conditional DNAT rules keyed on the concat sets; `sandbox_policy` shrinks to Envoy-egress allow rules only; atomic `nft -f` multi-table transaction; `policy_distributor` extended to inject into `sandbox_dnat` alongside `sandbox_policy`; UDP set stays effectively empty until a UDP use case lands (future-proofed, not load-bearing).
- Part 3 § "Listener design" — all three listeners bind on `NetworkInfo.gateway_ip` (not `127.0.0.1`, which would require `route_localnet=1`); TCP listener `:10001` with `getsockopt(SO_ORIGINAL_DST)` + `SO_LINGER {1, 0}` RST close + never `recv`; UDP listener `:10002` with `IP_RECVORIGDSTADDR` cmsg + no response; healthcheck listener `:10003` with `GET /health` (not in the nftables DNAT set — container-internal).
- Part 3 § "Hardening rules" — invariants 1–6: no peer-controlled bytes in event fields, fixed-size buffers, TCP RST close, UDP body discarded, per-session rate cap, per-session TCP concurrent-connection cap. Cap breaches surface as periodic summary events.
- Part 3 § "Liveness posture" — Docker `HEALTHCHECK` probes `/health`; sandboxd's gateway health polling observes container-unhealthy and restarts; `health_degraded` / `health_restored` lifecycle events bracket the outage.
- L3 amendment #2 implementation (doc-text edit lands in M10-S6).
- `networking/gateway/` wiring — new component process, `entrypoint.sh` update, Dockerfile changes if any, `docker run` bind-mount from sandboxd.

**Explicitly deferred.**

- Supervised restart of only the deny-logger process (entry point `networking/gateway/entrypoint.sh:113-220`) → future, per spec "Out of scope".
- nftables packet logging in `sandbox_policy`'s Envoy-egress chain → out-of-scope per spec.
- L3 spec text edit for amendment #2 → M10-S6.
- Default numeric values for rate / connection caps → M10-S6.

**Exit criteria.**

- cargo nextest green; `make test-integration` green; `make test-e2e` full suite green.
- New integration test (TCP): VM opens a connection to a non-allow-listed destination → RST close; a `deny` event with `orig_dst_ip`, `orig_dst_port`, `protocol: tcp`, `src_ip`, `src_port` lands in the per-session ring buffer.
- New integration test (UDP): VM sends a datagram to a non-allow-listed destination → `IP_ORIGDSTADDR` cmsg surfaces as `orig_dst_ip`; event lands in ring buffer; no response emitted.
- Integration test: exactly three nftables tables present after session start (`sandbox`, `sandbox_dnat`, `sandbox_policy`); `sandbox_policy` VM-egress reject rules are gone.
- HEALTHCHECK green; `health_degraded` / `health_restored` events emit around an intentionally-injected listener failure.
- `/review` pass clean; `make docs-build` green.

---

## M10-S4 — Event access surface: HTTP endpoint, `sandbox events` CLI, persistent sink

**Entry criteria.** M10-S3 complete.

**Spec reference.** `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — Part 3 § "Access surface" (HTTP endpoint, CLI); Part 3 § "Retention → Optional persistent sink"; Part 1 § "Discovery workflow" (end-to-end validation target).

**Rationale.** Exposes the ring buffer to operators. After this session the discovery workflow documented in Part 1 is usable end-to-end — `sandbox events --session=X --decision=deny --follow` surfaces every denial in real time and drives the iterative policy-tightening that replaces `Policy::unrestricted()`. The optional per-session per-layer JSONL persistent sink lands here so the first external consumer of the event stream can rely on it.

**In scope.**

- Part 3 § "HTTP endpoint" — `GET /sessions/{session_id}/events` on the existing Unix-socket API with repeatable `layer` / `decision` / `event` filters and `since` (RFC 3339); `follow=false` returns bounded JSONL from the ring buffer; `follow=true` streams via chunked JSONL (SSE deferred per spec).
- Part 3 § "CLI" — `sandbox events` subcommand as a thin client; repeatable filter flags matching the HTTP shape; `--json` (default) and `--table` output modes; shell-redirect persistence path (`sandbox events --follow > file.jsonl`).
- Part 3 § "Retention → Optional persistent sink" — `events.persist` + `events.persist_retention_days` config knobs; on-disk layout `{base_dir}/sessions/{id}/events/{layer}-YYYY-MM-DD.jsonl`; UTC-midnight rotation; retention pruner; explicit non-use of `sessions.db` for event volume.
- API DTO boundary — event HTTP responses go through a dedicated DTO module, not flattened domain structs.

**Explicitly deferred.**

- SSE transport → spec "Out of scope".
- Rotated-file compression → spec "Out of scope".
- Daemon-level (non-session) events endpoint → spec "Known gaps".

**Exit criteria.**

- cargo nextest green; `make test-integration` green; `make test-e2e` full suite green.
- New E2E test exercising the discovery workflow end-to-end: create session with empty policy, run a workload, `sandbox events --session=X --decision=deny --follow` surfaces the S3 deny events with host/port/protocol/layer; a subsequent `sandbox policy update` closes the matching denials on the next workload run (exercises S2 ingestion → S3 deny-logger → S4 CLI).
- Persistent-sink integration test: with `events.persist: true`, a rotated JSONL file appears under `{base_dir}/sessions/{id}/events/`; the retention pruner removes files older than the configured window.
- `/review` pass clean; `make docs-build` green.

---

## M10-S5 — CLI-side preset system and orphaned-helper cleanup

**Entry criteria.** M10-S4 complete.

**Spec reference.** `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — Part 2 in full (Semantics, CLI shape, Built-in presets, User-configured presets, Preset versioning); "Cleanup sites → Orphaned daemon-side preset helpers"; "Known gaps" bullets on `cargo` preset host verification and exact `github-repo` / `github-pr` path lists.

**Rationale.** Replaces `Policy::unrestricted()`'s convenience role with a principled client-local preset system and closes the user-facing preset gap. Presets are fully in `sandbox-cli`; the daemon only ever sees expanded v2 rules and a `source_presets` array (landed on `policy_applied` / `policy_updated` from S2). Orphaned daemon-side helpers (`preset_allow_github`, `preset_allow_npm` in `sandbox-core/src/policy.rs`) are deleted in the same pass that introduces their CLI replacements — no window where preset functionality is missing.

**In scope.**

- Part 2 § "Semantics" — client-local expansion, union merge, uniqueness validation with source-naming error shape (from Part 1), `source_presets` transmission to daemon, daemon re-validation as defensive check.
- Part 2 § "CLI shape" — `--preset 'name:key=val,...'` repeatable flag on `sandbox start` and `sandbox policy update`; trailing-colon for unparameterized presets; repeated keys for multi-value params; comma/colon/equals-avoidance rule documented.
- Part 2 § "CLI shape → Discovery and inspection" — `sandbox policy preset list | show | expand` read-only client-local subcommands; `expand` as pure dry-run (no daemon contact).
- Part 2 § "CLI-side preset loading errors" — malformed user file → stderr warning + skip; unknown preset invocation → hard error before daemon contact; missing XDG dir → empty, no error; unreadable dir metadata → stderr warning + empty set.
- Part 2 § "Built-in presets → Unparameterized ecosystem presets" — full table (`npm`, `pypi`, `cargo`, `goproxy`, `maven`, `gradle`, `dockerhub`, `github` read-oriented rows, `github` interactive-surfaces row); method-filter posture split (GET+HEAD for consume-only; ANY for interactive GitHub surfaces); accepted mitmproxy-in-large-blob-path tradeoff.
- Part 2 § "Built-in presets → Parameterized github presets" — `github-repo` (repeatable `repo=owner/name`, full path-template list), `github-pr` (paired repeatable `repo=` / `pr=`, stricter subset). Exact `http_filters` path lists authored here with test coverage (closes spec known-gap).
- Part 2 § "User-configured presets" — XDG loading from `$XDG_CONFIG_HOME/sandboxd/presets/*.json` (fallback `$HOME/.config/sandboxd/presets/`); `${param_name}` string substitution only; max one repeatable param per user preset; built-in shadowing rejected with an error naming both sources.
- Part 2 § "Preset versioning" — no version field; ship-with-CLI-release semantics documented.
- `cargo` preset empirical verification — live `cargo fetch` trace against an empty cache; confirm or correct the `crates.io` / `index.crates.io` / `static.crates.io` host list (closes spec known-gap).
- "Cleanup sites → Orphaned daemon-side preset helpers" — delete `preset_allow_github` at `sandbox-core/src/policy.rs:391-411` and `preset_allow_npm` at `:413-436`; remove any call sites and test fixtures in the same pass.

**Explicitly deferred.**

- Git LFS coverage (`github-lfs` preset or `lfs=true` param on `github-repo`) → future, per spec "Known gaps".
- User-preset versioning scheme → future, per spec "Known gaps".

**Exit criteria.**

- cargo nextest green; `make test-integration` green; `make test-e2e` full suite green.
- New E2E test per preset class: `sandbox start --preset 'npm:'` → `npm install` of a small package inside the VM succeeds; a host outside the preset fails with a visible deny event through the S4 CLI.
- `sandbox policy preset expand 'github-repo:repo=foo/bar'` emits v2 rules that round-trip through the daemon's re-validation successfully.
- `sandbox policy preset expand 'cargo:'` followed by `cargo fetch` in a fresh VM succeeds; the resulting host list matches the trace-verified set.
- Grep sweep: `preset_allow_github` / `preset_allow_npm` return zero matches anywhere in the tree.
- `/review` pass clean; `make docs-build` green.

---

## M10-S6 — L3 spec amendments, docs sweep (mandated and general), grep audit, measurement defaults

**Entry criteria.** M10-S5 complete.

**Spec reference.** `.tasks/specs/2026-04-19-l3-envoy-mitmproxy-flow-design.md` (amendment target); `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` — "Amendments to the L3 spec and its landed implementation" § 1–3, "Known gaps / deferred decisions" (measurement-defaults bullet).

**Rationale.** Lands documentation alignment, site-wide consistency, and measurement-driven defaults before spec-delivery verification in M10-S7. S6 makes the docs match the code; S7 will make the code match the spec. The L3 spec text is brought in line with shipped code (amendments #1 and #2 are doc edits here; #3 is code-only and already landed). The user-facing docs site gets a full review — not just the files the spec names, but a site-wide walk catching stale language, inconsistencies, and gaps that surface now that M10's features are live. Default values for the ring buffer size, retention window, and deny-logger caps are picked against a measured workload. `progress.json` todos accumulated across S1..S5 are closed in-branch or re-logged with explicit target milestones.

**In scope.**

- Amendments to the 2026-04-19 L3 spec text — edit #1 (replace "No port predicate on any L3 chain" with the 2026-04-21 spec's prescribed wording); edit #2 (narrow "kept unchanged: `sandbox_policy`" to "Envoy-egress rules in `sandbox_policy` are kept unchanged"); re-read the L3 spec end-to-end and fix any other derived statement that is no longer accurate.
- Spec-mandated user-facing doc updates — removals of `--unrestricted`, bare-`*` host, `protocol: any/http/https`, and silent-reject semantics across `docs/guides/network-policies.md`, `docs/concepts/networking.md`, `docs/concepts/architecture.md`, `docs/concepts/policy-model.md`, `docs/guides/troubleshooting.md`; new preset docs (Part 2) and events docs (Part 3) written where they do not exist.
- **General docs site review — site-wide pass beyond the spec's prescribed files.** Walk every page under `docs/` (per the taxonomy established in M9-S16/S17) and check: accuracy against current code behavior, internal consistency with the newly-written preset / events / deny-logger content, stale references to removed APIs (v1 schema, unrestricted, old protocol values, transparent-mode mitmproxy, `sandbox_l3` leftovers if any), and cross-link freshness. Findings are fixed in-branch or filed as user-approved `progress.json` todos with a named target milestone.
- Grep audit — zero matches for: `Policy::unrestricted`, `is_unrestricted`, `--unrestricted`, bare-`*` host literals in compiler arms, `dport { 80, 443 }`, `fnmatch.fnmatchcase`, `preset_allow_github`, `preset_allow_npm`, `protocol: any` / `protocol: http` / `protocol: https` in sample policies, v1 schema references in docs. Surviving matches are acceptable only with an inline rationale in rationale/historical comments.
- Measurement-driven defaults — pick concrete numeric values for `events.ring_buffer_size`, `events.persist_retention_days`, the deny-logger per-session rate cap, and the per-session TCP concurrent-connection cap, backed by a representative-workload measurement. Document the methodology in a short note alongside the spec's known-gaps section or in an adjacent design note.
- `progress.json` todo sweep — every user-approved todo accumulated across M10-S1..S5 is closed in-branch here or re-logged with an explicit target milestone (not silently left).

**Explicitly deferred.**

- Git LFS preset (`github-lfs` / `lfs=true`) → follow-on milestone, per spec "Known gaps".
- Cross-layer event correlation → spec "Out of scope" (~40h by spec estimate).
- Rotated-event-file compression and SSE transport → spec "Out of scope".
- User-configured preset versioning → spec "Known gaps".
- Daemon-level (non-session) events endpoint → spec "Known gaps".
- Supervised deny-logger subprocess restart → spec "Out of scope".

**Exit criteria.**

- 2026-04-19 L3 spec contains amendments #1 and #2 as prescribed; L3 spec re-read end-to-end confirms no other derived statement is now false.
- Site-wide docs review complete: every page under `docs/` has been re-read; findings fixed in-branch or re-logged as todos with clear ownership.
- Grep audit returns zero matches for every negative pattern; any surviving match has an inline rationale comment.
- `make docs-build` green; `starlight-links-validator` clean; Mermaid diagrams render as build-time SVG and as native markdown on GitHub.
- Default values for the four measurement knobs selected with a documented methodology; values committed to config defaults.
- `make test-e2e` full suite green.
- `/review` (or `kio-reviewer`) run over the full diff; every finding resolved in-branch.
- **Hard gate** — do not mark complete unless ALL of: (a) L3 spec amended and re-read; (b) docs site walked end-to-end; (c) `/review` clean; (d) `make test-e2e` green; (e) `progress.json` sweep closed.

---

## M10-S7 — Self-critical spec-delivery verification

**Entry criteria.** M10-S6 complete.

**Spec reference.** `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-design.md` in full (all parts, all sections); `.tasks/specs/2026-04-19-l3-envoy-mitmproxy-flow-design.md` for amendment verification.

**Rationale.** Sessions S1–S6 decompose the spec into PR-sized increments, each with its own bullets and gates. That decomposition creates an incentive gap: an agent closes a session when its bullets are checked, not when the spec's claims are provably delivered. This session closes the gap with a single artifact — a claim-to-code verification map produced by re-reading the 2026-04-21 spec as if unfamiliar with M10, and mapping every concrete claim to either shipped code + test, an "Out of scope" item, or a user-approved `progress.json` todo. Framing shift from S6: S6 makes docs match the code; S7 makes the code match the spec. S7 is the milestone's final gate and produces the only per-session deliverable that is an artifact committed to the repo rather than code changes.

**In scope.**

- Claim-to-code verification — re-read the 2026-04-21 spec end to end, approaching each section without assuming M10 delivered it. For every concrete claim (rule-shape fields, validator error shapes, compiler behaviors, migration steps, event envelope fields, CLI command surfaces, preset expansions, nftables chain shapes, listener protocols, lifecycle event triggers, retention semantics), record one of:
  - (a) code locator (file:line) + test locator (file:test-name) that implements and exercises the claim;
  - (b) the spec's own "Out of scope" bullet that excludes it;
  - (c) a user-approved `progress.json` todo with a named target milestone.
- Example verification — run every explicit example the spec prescribes (Part 1 rule-shape JSON, the duplicate-destination error text, Part 2 CLI invocations, Part 3 event-envelope JSON, the `prerouting` nft chain excerpt) and confirm output matches character-for-character where the spec is specific. Captured deltas are either fixed in-branch or logged.
- Out-of-scope conformance — confirm every item in the spec's "Out of scope" section is genuinely absent from the tree (no dead code, no surface expansion that quietly implements an excluded item).
- Known-gap tracking — confirm every item in "Known gaps / deferred decisions" is tracked in `progress.json` or filed to a named follow-on milestone. No silent drops.
- L3-amendment verification — amendments #1, #2, #3 each have a concrete code locator AND, for #1 and #2, the corresponding L3 spec text edit in `.tasks/specs/2026-04-19-l3-envoy-mitmproxy-flow-design.md`. Confirm neither divergence nor drift.
- Delta resolution — any claim without a valid (a / b / c) mapping is a blocker. Fix in-branch or file a user-approved todo with target milestone before session close.

**Explicitly deferred.**

- New feature work, refactors, doc polish — S7 is a verification pass; it does not introduce new behavior.
- Future-milestone items already captured as `progress.json` todos or spec "Out of scope" bullets.

**Exit criteria.**

- Claim-to-code verification map committed to the repo at `.tasks/specs/2026-04-21-port-explicit-policies-presets-observability-delivery.md` (or an adjacent location), with every concrete spec claim mapped to exactly one of (a), (b), or (c).
- Every spec example prescribing specific output has been run and matches (or the delta is captured in-branch / logged).
- Out-of-scope conformance check complete; zero items accidentally implemented.
- Known-gap bucket reconciled — every item in `progress.json` or in a named follow-on milestone.
- L3 amendments #1 / #2 / #3 verified against shipped code AND (for #1 / #2) against the L3 spec text.
- `make test-e2e` full suite green on two consecutive runs (mirroring the M9-S20 final-session gate).
- `/review` (or `kio-reviewer`) run over the full diff; every finding resolved in-branch.
- **Hard gate** — do not mark complete unless ALL of: (a) verification map committed with every claim mapped; (b) every prescribed example run and matched; (c) out-of-scope conformance verified; (d) known-gap reconciliation recorded; (e) L3-amendment verification complete; (f) `/review` clean; (g) two consecutive full-suite E2E runs green.
