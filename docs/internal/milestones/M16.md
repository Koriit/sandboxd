# M16 — Update infrastructure

Spec 5 closes the five-spec arc. It adds the `sandbox update` CLI subcommand (the only supported in-place upgrade path after Spec 4's install.sh), the config migration framework that applies Spec 1's V001 transform and future transforms to `/etc/sandboxd/users.conf` and `/etc/qemu/bridge.conf`, the lock-file mutex that guards concurrent updates, the backup mechanics that make the documented manual rollback recipe possible, and the daemon-side schema-mismatch refusal that closes the safety loop. M16 is the terminal milestone for Spec 5 and for the entire five-spec arc. M16-S6 was added post-S5 to close the Spec 2 § 3.8.1 production gap (`docker cp` against `--read-only` lite containers) that surfaced during M13-S5 and remained queued as deferred todos #141/#142/#147/#154 through M14–M16 — the resolution is a container-side guest-binary delivery change (host-side bind-mount of a staged file, replacing `docker cp` into the rootfs) that is too narrow to warrant reopening M13 and lands naturally here as the actual close of the arc. M16-S7/S8/S9 follow as a three-way split of the remaining post-arc fix-up backlog by theme — code, tests, and docs respectively — surfaced by the M13–M16 multi-track reviews and the 2026-05-15 deferred-todo walkthrough; each session pulls in the matching subset of the M16-S4 MAY-FIX triage plan at `.tasks/handoffs/m16-s4-triage-plan.md`.

**Spec references:**

- [`../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md`](../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md) — M16-S1..S3, M16-S4..S5, M16-S7 (§ 2.3, § 5), M16-S9 (no direct sections; cross-referenced for rollback recipe context)
- [`../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md`](../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md) — M16-S6 (Spec 2 § 3.8.1 amendment), M16-S8 (§ 7.5, § 9.2), M16-S9 (§ 2.2, § 5)
- [`../specs/2026-05-11-helper-identity-assertion-design/2026-05-11-helper-identity-assertion-design-spec.md`](../specs/2026-05-11-helper-identity-assertion-design/2026-05-11-helper-identity-assertion-design-spec.md) — M16-S8 (§ 8.4, § 3.5 / P8.20), M16-S9 (§ 3.5)
- [`../specs/2026-05-11-daemon-productionization-design/2026-05-11-daemon-productionization-design-spec.md`](../specs/2026-05-11-daemon-productionization-design/2026-05-11-daemon-productionization-design-spec.md) — M16-S7 (§ 7.3, § 12.2, § 13.2), M16-S8 (§ 11.6), M16-S9 (§ 6, § 12)
- [`../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md`](../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md) — M16-S7 (§ 4.6), M16-S8 (§ 6, § 7 amendment for local Sigstore stack), M16-S9 (§ 4.2)

---

## M16-S1 — Config migration framework + daemon schema-mismatch refusal + `rebuild-image --backend gateway` refusal

**Entry criteria.** Two independent gates, both required:

1. **M14 complete** (Spec 3) — `load_users_config()` and `ensure_base_dir_layout` call sites must exist in `sandboxd/src/main.rs` before `validate_users_conf_schema_version` can be wired in immediately after the config load; the `SandboxError` / `UsersConfigError` mapping machinery must be in place.
2. **M15 complete** (Spec 4) — `scripts/install.sh` must exist so that `scripts/lib.sh` has source material to extract the cosign pin constants from; Spec 4's tarball layout and MANIFEST format must be finalised so the cosign constants in `lib.sh` match what the release pipeline produces and what M16-S2's fetch/verify logic must verify.

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — §§ 4.1–4.7 (config migration framework: registry location, `ConfigMigration` trait, apply loop, atomic write semantics, version detection per file type, preserving operator content, daemon-side schema-mismatch refusal), § 8 (`rebuild-image --backend gateway` refused), § 13 (implementation notes: `cfg_migrations/`, `users_conf.rs`, `bridge_conf.rs`, `main.rs` wiring).

**Rationale.** The migration framework and daemon schema validation are pure Rust library code with no shell scripting and no privileged operations — they are the most isolated deliverable in Spec 5 and the natural starting point. The daemon-side `validate_users_conf_schema_version` call ships here because it must precede the update orchestration (M16-S2): the validator is the convergence anchor that forces operators to run `sandbox update` when the config file drifts behind the binary's supported version. The `rebuild-image --backend gateway` refusal is a one-variant CLI change that belongs alongside the framework it documents as the "right path instead."

**In scope.**

- `sandboxd/sandbox-cli/src/cfg_migrations/mod.rs` per Spec 5 § 4.1–4.3: `ConfigMigration` trait (Spec 5 § 4.2 — `id()`, `name()`, `target_file()`, `from_version()`, `to_version()`, `apply(&[u8]) -> Result<Vec<u8>, MigrationError>`), `TargetFile` enum (`UsersConf`, `BridgeConf`) with `canonical_path()`, `MigrationError` variants (`Io`, `Parse`, `Transform`, `Validation`, `SchemaUnreadable`), static `registry()`, `pending()`, `latest_for()`, `apply_pending(TargetFile) -> Result<Vec<u32>, MigrationError>` per Spec 5 § 4.3.
- `atomic_write(path, bytes)` per Spec 5 § 4.4: `NamedTempFile::new_in(parent)` + `persist(path)` — same-FS rename guarantees no half-written state.
- `sandboxd/sandbox-cli/src/cfg_migrations/version.rs` per Spec 5 § 4.5: `read_schema_version(&[u8], TargetFile) -> Result<u32, MigrationError>` for JSON `_schema_version` (users.conf) and `# sandbox-schema-version: <int>` first-line header (bridge.conf).
- `sandboxd/sandbox-cli/src/cfg_migrations/v001_add_sandbox_to_allow_users.rs` per Spec 5 §§ 4.1, 9.2: adapter `impl ConfigMigration` calling `sandbox_core::users_conf::migrate_v001` (Spec 1 § 5). Does **not** move or duplicate the pure transform — it wraps it.
- `--apply-config-migration` and `--dump-migration-set` hidden CLI affordances per Spec 5 § 4.3 / § 3.1.4: both gated as described in § 4.3's access-gating block (root check, canonical-path validation for `--file`, tempfile-parent validation for `--out`, registry ID validation for `--migration`; `--dump-migration-set` is read-only, no path arguments).
- `sandboxd/sandbox-core/src/users_conf.rs` additions per Spec 5 § 4.7: `DAEMON_MAX_SUPPORTED_USERS_CONF_SCHEMA: u32 = 1`, `DAEMON_MIN_SUPPORTED_USERS_CONF_SCHEMA: u32 = 1`, `UsersConfigError::SchemaTooNew { file_version, daemon_max, hint }` + `SchemaTooOld { .. }`, `pub fn validate_users_conf_schema_version(&UsersConfig) -> Result<(), UsersConfigError>`.
- `sandboxd/sandbox-core/src/bridge_conf.rs` per Spec 5 § 13: small new module, `DAEMON_MAX_SUPPORTED_BRIDGE_CONF_SCHEMA: u32 = 0`, `read_bridge_conf_schema_version(&[u8]) -> Result<u32, _>`, `validate_bridge_conf_schema_version()` (v1 no-op validator — only header-present-but-zero passes). The daemon reads `bridge.conf` for this check only; it does not otherwise parse the file.
- `sandboxd/sandboxd/src/main.rs` wiring per Spec 5 § 4.7: call `validate_users_conf_schema_version(&users_config)` immediately after `load_users_config()` at line 6156; call `bridge_conf::validate_schema_version()` at the same site; emit clear stderr and return `Err` on mismatch.
- `sandboxd/sandbox-core/src/error.rs` per Spec 5 § 13: map `UsersConfigError::SchemaTooNew`/`SchemaTooOld` through to `SandboxError` + clear `Display`.
- `sandbox rebuild-image --backend gateway` refusal per Spec 5 § 8.1: add `Gateway` variant to `RebuildImageBackend` enum; dispatch arm exits `2` with the verbatim error text pointing at `sandbox update`.
- `scripts/lib.sh` per Spec 5 § 3.1.9: factor cosign pin constants (`COSIGN_VERSION`, `COSIGN_SHA256_AMD64`, `COSIGN_SHA256_ARM64`) from `scripts/install.sh` into a shared `scripts/lib.sh`; update `install.sh` to source it. The same lib.sh is sourced by the `sandbox update` shell paths in M16-S2/S3.
- Unit tests per Spec 5 § 9.2: `migration_v001_round_trip` (5 table rows including operator-customized), `migration_v001_idempotent_when_already_applied`, `read_schema_version_*` (6 tests), `apply_pending_walks_chain`, `apply_pending_skips_already_at_target`, `apply_pending_atomic_write_visible_only_after_complete`, `registry_migrations_advance_exactly_one_version`, `apply_config_migration_refuses_non_root_caller`, `apply_config_migration_refuses_non_canonical_file`, `apply_config_migration_refuses_arbitrary_out_path`, `apply_config_migration_refuses_unknown_migration_id`, `validate_users_conf_schema_version_accepts_supported`, `validate_users_conf_schema_version_rejects_too_new`, `validate_users_conf_schema_version_rejects_too_old`, `validate_users_conf_schema_version_treats_absent_as_zero`, `rebuild_image_gateway_backend_refuses_with_pointer_to_update`, `rebuild_image_container_backend_sends_correct_body`, `rebuild_image_lima_backend_sends_correct_body`.
- Integration tests per Spec 5 § 9.3: `integration_config_migration_applies_v001_to_legacy_file`, `integration_daemon_refuses_start_on_schema_too_new`, `integration_daemon_refuses_start_on_schema_too_old`, `integration_daemon_accepts_start_on_schema_at_max`.

**Explicitly deferred.**

- `sandbox update` orchestration → M16-S2 (pre-flight) + M16-S3 (stateful flow).
- Lock file → M16-S2.
- Backup mechanics → M16-S3.
- Lima E2E tests for update scenarios → M16-S3.

**Exit criteria.**

- All Spec 5 § 9.2 unit tests in scope pass under the default nextest profile.
- All Spec 5 § 9.3 integration tests in scope pass under the `integration` nextest profile.
- `registry_migrations_advance_exactly_one_version` fails when a synthetic migration with `to_version() != from_version() + 1` is injected into a test-only registry.
- `--apply-config-migration` with a non-root caller exits non-zero with substring `requires root`; `--apply-config-migration --file /tmp/fake.json` exits non-zero with substring `canonical paths`; same for out-path and migration-ID violations.
- Daemon startup with `users.conf` at `_schema_version: 99` exits non-zero with stderr substring `users.conf schema version 99 is newer`.
- `sandbox rebuild-image --backend gateway` exits `2` with substring `sandbox update`.
- `--dump-migration-set` exits `0` and prints a JSON array of migration entries each containing at least `id`, `from_version`, `to_version`, and `target_file` fields — the shape M16-S2's `--dry-run` stopped-session classification depends on.
- `shellcheck -s sh -S style scripts/lib.sh scripts/install.sh` passes.
- `cargo nextest run --workspace` default and integration profiles clean; `cargo clippy --workspace` clean; code review approved.

---

## M16-S2 — Lock file + `sandbox update` pre-flight + `--check`/`--dry-run` modes

**Entry criteria.** M16-S1 (the migration framework must exist for the pre-flight's migration dry-run to call; `--dump-migration-set` must exist for the `--dry-run` stopped-session classification; `scripts/lib.sh` must exist for the cosign bootstrap to source it).

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — §§ 2.1–2.5 (CLI surface, `--check` output, `--dry-run` output, confirmation prompt, privilege model), § 2.6 (log file destination — same file as install.sh), §§ 3.1.1–3.1.12 (pre-flight steps: arg parse, dev-mode detect, install state read, target version + fetch, version compare with `--check` exit gate, active sessions check, stopped session compat enumeration, disk space, cosign bootstrap, sigstore verify + extract, migration dry-run, confirmation prompt), §§ 6.1–6.4 (lock file: path/shape, acquisition shell pseudo-code, ordering rule flock-before-payload, acquisition outcomes, release, sticky `was_running` flag), §§ 10.1, 10.3 (lock mode rationale, self-replacement semantics).

**Rationale.** The read-only half of `sandbox update` — the pre-flight, `--check`, `--dry-run`, and the lock mechanics — forms a self-contained unit whose exit criteria do not require stopping the daemon or writing backups. Separating it from the stateful steps (M16-S3) gives reviewers a focused PR: "does `--check` produce the right output without side effects? does the lock correctly serialize concurrent invocations?" The lock file module is included here (not S3) because it is acquired as the first stateful step and its unit tests are hermetic.

**In scope.**

- `sandboxd/sandbox-cli/src/update/lock.rs` per Spec 5 §§ 6.1–6.4: lock-file path (`/var/lib/sandbox/.update.lock`, mode `0664 sandbox:sandbox`), acquisition (non-blocking `flock -n -x` via shell invoked from Rust `Command`, JSON payload write, dead-PID adoption with sticky `was_running`, stale-payload detection at >24h), release (`rm -f + exec {fd}>&-`). Unit tests: `lock_file_acquisition_refuses_on_live_holder`, `lock_file_acquisition_adopts_on_dead_pid_payload`, `lock_file_acquisition_preserves_was_running_across_adopt`, `lock_file_flock_acquired_before_payload_write`, `lock_file_released_on_process_exit`.
- `sandboxd/sandbox-cli/src/update/fetch.rs` per Spec 5 §§ 3.1.4, 3.1.9, 3.1.10: release tarball fetch (GitHub Releases API for latest-tag resolution, `--from` local path, `--source-url` mirror), cosign bootstrap (downloads cosign v2.4.1, sha256-verifies, stores in tmpdir; sources constants from `scripts/lib.sh` via a Rust `include_str!` constant or a parallel `const`-duplication with a comment pointing at lib.sh), sigstore `verify-blob`, tarball extraction, MANIFEST sanity + per-file sha256 verification. MANIFEST arch cross-checked against install-state `installed_arch`, not `uname -m`.
- `sandboxd/sandbox-cli/src/update/mod.rs` pre-flight logic per Spec 5 §§ 3.1.1–3.1.12: full arg-parse (superset of install.sh flags), dev-mode detection and refusal, install-state read, target-version determination, version compare (up-to-date short-circuit), `--check` early-exit gate, active sessions query, stopped session compat enumeration using current daemon's `DAEMON_GUEST_PROTO_VERSION` (for `--check`) and target binary's constants via `--dump-migration-set` (for `--dry-run`), disk-space check, cosign bootstrap delegation to `fetch.rs`, sigstore verify + extract, migration dry-run via `apply_pending` in memory (read + transform + validate, no write), confirmation prompt (`Proceed? [y/N]:`, `--yes` bypass, `--dry-run` prints plan + exits 0).
- `Command::Update { ... }` variant in `sandbox-cli/src/main.rs` per Spec 5 § 13; dispatch arm calling `update::run(args)`.
- `--check` output verbatim per Spec 5 § 2.2 (including exit codes: 0=up-to-date, 1=error, 2=arg-parse-failure, 3=update-available).
- `--dry-run` output verbatim per Spec 5 § 2.3 (would-execute/skip labels, § 3 step numbers referenced).
- Update log appended to `/var/log/sandbox-install.log` with `sandbox-update` second token per Spec 5 § 2.6.
- Unit test `version_lifecycle_check_then_dry_run_then_apply` (mocked daemon `/version`; assert each phase output shape; assert no privileged calls in `--check`/`--dry-run`).

**Explicitly deferred.**

- Stateful steps §§ 3.2.13–3.2.30 (stop daemon, backup, install binaries, start daemon) → M16-S3.
- Lima E2E tests → M16-S3.

**Exit criteria.**

- All lock-file unit tests pass under the default nextest profile.
- `sandbox update --check` from a lower version reports "Status: update available", exits `3`, and does not acquire the lock (lock file absent after invocation).
- `sandbox update --check` at current version reports "Status: up to date", exits `0`.
- `sandbox update --dry-run` prints the full plan with `would execute`/`skip` labels for all 18 stateful steps, exits `0` when all pre-flight passes.
- `sandbox update --from <tarball>` with a mismatched arch (per MANIFEST) dies with "MANIFEST arch mismatch" before any state mutation.
- `sandbox update` (no flags) after a successful pre-flight presents the confirmation prompt with `Proceed? [y/N]:` and exits without acting on `N`.
- `cargo nextest run --workspace` default profile clean; `cargo clippy --workspace` clean; code review approved.

---

## M16-S3 — `sandbox update` stateful flow + backup mechanics + Lima E2E tests

**Entry criteria.** M16-S2 (pre-flight and lock acquisition must exist before the stateful steps can be wired; the confirmation prompt must exist so the gate before stateful changes is in place).

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — §§ 3.2.13–3.2.30 (all stateful steps verbatim: acquire lock through release lock), §§ 3.3–3.4 (preserved artefacts, failure handling / idempotency table), §§ 5.1–5.5 (backup mechanics: layout, retention policy, manifest format, `.bak` not in PATH, operator access), § 7 (documented rollback recipe — publish as docs, not implement), § 9.1 (Lima E2E test matrix), § 11 (dev-mode compatibility), § 13 (implementation notes: `update/backup.rs`, `update/migrate.rs`, Lima E2E test files).

**Rationale.** The stateful steps and backup module are the heart of Spec 5: this is where the daemon is stopped, the backup set is captured, binaries are swapped, config migrations are applied, and the daemon is verified and restarted. The Lima E2E tests are included here because they exercise the full update flow end-to-end in a real VM — they are the primary correctness gate for the stateful steps, and running them against only a partial implementation would give false confidence.

**In scope.**

- `sandboxd/sandbox-cli/src/update/backup.rs` per Spec 5 §§ 5.1–5.5: backup set directory creation at `/var/lib/sandbox/backups/<ISO8601>-from-<v1>-to-<v2>/` (as `sandbox` user); sessions.db backup with sha256-compare idempotency; `/etc` files backup (`users.conf`, `bridge.conf`, each at `0644`, via `sudo -k cat | sudo -k -u sandbox tee` pipeline); binary backups at mode `0640` (not executable); `manifest.json` written in-progress at § 3.2.19 with `completed_ok: false`; retention prune at § 3.2.25 (keep last 2 `completed_ok: true` sets; never prune in-progress or failed sets); finalization at § 3.2.29 (`completed_ok: true`, `completed_at` set).
- `sandboxd/sandbox-cli/src/update/migrate.rs` per Spec 5 §§ 4.3, 3.2.24: wrapper around `cfg_migrations::apply_pending` that orchestrates the sudo-elevation pattern for file writing (CLI calls `apply_pending` in-process for the transform, then `sudo -k mv` for the atomic rename, using tempfiles at canonical paths like `/etc/sandboxd/.users.conf.tmp.V001`).
- Full stateful orchestration in `update/mod.rs` per Spec 5 §§ 3.2.13–3.2.30: all 18 steps wired in order with the binding image-load-before-binary-swap sequence (§ 3.2.19 ordering note), idempotency checks per step, log lines per step to `/var/log/sandbox-install.log`.
- `§ 3.2.18` install-state `previous_version` field update (new optional field; written before binary swap per § 3.2.18; `jq`-safe on read with `// ""`).
- `§ 3.2.27` post-start verification: 30-second socket-appearance wait loop, `/version` query via `curl --unix-socket`.
- `§ 3.2.28` doctor invocation: `sandbox doctor --verbose` exit-code gate; failure points at backup-set manifest.
- Rollback recipe documentation per Spec 5 § 7.2: a copy-pasteable shell sequence published in `docs/`; not automated by CLI.
- `docs/start/installation.md` update per Spec 5 § 13: "To upgrade, run `sudo sandbox update`."
- Lima E2E tests in `tests/install-e2e/test_update_*.py` per Spec 5 § 9.1 (11 test cases): `test_update_fresh_install_to_next_version`, `test_update_interrupted_then_resumed`, `test_update_then_manual_rollback`, `test_update_air_gapped`, `test_update_check_does_not_mutate`, `test_update_concurrent_refused`, `test_update_preserves_customized_users_conf`, `test_update_preserves_systemd_drop_in`, `test_update_with_recreate_session_classification`, `test_update_rejects_dev_install`, `test_update_backup_retention_prunes_oldest`, `test_update_partial_failure_backup_set_preserved`.
- Integration test per Spec 5 § 9.3: `integration_update_flow_idempotent` (stub install in temp tree, simulated update, second run all-skip).

**Explicitly deferred.**

- Automated `sandbox rollback` subcommand → Spec 5 § 12 (explicitly out of scope in v1; rollback is the documented manual recipe).
- `/etc/profile.d/sandbox.sh` for `SANDBOX_SOCKET` → deferred per Spec 4 § 10.7 (already documented as follow-up in M15-S5).

**Exit criteria.**

- All Spec 5 § 9.3 integration tests pass under the `integration` nextest profile.
- All 11+ Lima E2E tests in `test_update_*.py` pass locally on a KVM-capable host.
- `test_update_check_does_not_mutate`: after `--check`, the lock file path is confirmed absent by direct `os.path.exists` check (not inferred from a log line), install state `installed_version` field matches the pre-check value, daemon `/version` reports the old version.
- `test_update_interrupted_then_resumed`: second run's log has mostly `action=skip` lines; final state converges to target version.
- `test_update_preserves_customized_users_conf`: operator-added subnet survives the update cycle byte-for-byte.
- `test_update_preserves_systemd_drop_in`: drop-in file is bit-for-bit unchanged after update.
- `test_update_backup_retention_prunes_oldest`: exactly 2 backup sets remain after 3 successful upgrades; in-progress/failed sets are never pruned.
- Rollback recipe documentation exists and is copy-pasteable (verified by tracing through it manually in a Lima VM).
- `docs/start/installation.md` updated.
- `cargo nextest run --workspace` default and integration profiles clean; `cargo clippy --workspace` clean; code review approved.

---

## M16-S4 — Multi-track review

**Entry criteria.** M16-S3 complete — all three implementation sessions landed; `cargo nextest run --workspace` default and integration profiles pass; Lima E2E harness tests pass.

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` (all sections).

**Rationale.** The update command is the most security- and reliability-sensitive deliverable in the arc: it runs with `sudo`, stops the daemon, swaps binaries, and applies config transforms — all while maintaining idempotency. The six review tracks are calibrated to catch failure modes specific to this scope (TOCTOU races in the lock, partial-backup states that break the retention filter, config migration transforms that silently drop operator content, security gating on the hidden CLI flags).

**In scope.**

Six tracks, each delegated to a separate review agent in parallel:

- **Track 1 — Implementation vs. spec**: Re-read Spec 5 §§ 1–11 section by section; compare every concrete requirement (step order, log line format, file mode, JSON field name, error message token, lock-file acquisition outcome, retention filter predicate, atomic-write guarantee) against the implementation; flag any gap or divergence.
- **Track 2 — Code quality**: Review the `update/` module, `cfg_migrations/`, `lock.rs`, `backup.rs`, and `migrate.rs` for: ownership/borrowing correctness in the lock acquisition (no FD closed prematurely), correct use of `std::process::Command` for all privileged `sudo -k` calls, error propagation (no silent `unwrap` in the error path of stateful steps), idiomatic Rust. Check that every `sudo -k` elevation has a corresponding log line before it fires. Review the hidden subcommand security gates for completeness (all four conditions from § 4.3).
- **Track 3 — Unit test quality**: For every unit test in §§ 9.2 covering the lock, framework, schema-mismatch, and subcommand gates: verify the assertion is non-tautological (the tested property is not reachable without the production code path). Run `cargo-llvm-cov` on `cfg_migrations/`, `update/lock.rs`, `update/backup.rs`, and `sandbox-core/src/users_conf.rs`; identify uncovered lines and document each with a reason or a new test. Specifically: verify `apply_pending_atomic_write_visible_only_after_complete` actually injects a fault and observes the pre-write state (not just a successful path with a comment).
- **Track 4 — E2E / integration test quality**: For each Lima E2E test in `test_update_*.py`, verify the assertion covers the full observable post-condition (not just exit code): `test_update_interrupted_then_resumed` must parse the second run's log and count `action=skip` lines; `test_update_then_manual_rollback` must verify daemon version, sessions.db row count, and doctor exit code post-rollback; `test_update_concurrent_refused` must read stderr and verify the "in progress" message substring. Verify `test_update_check_does_not_mutate` inspects the lock file path directly (not inferred). Check that `test_update_backup_retention_prunes_oldest` counts the backup-set directories, not just trusts the prune log.
- **Track 5 — Docs quality**: Verify the rollback recipe in `docs/` is copy-pasteable without modification on a standard Ubuntu 22.04 VM (trace through each step; confirm path names, file modes, and tool invocations match the spec's § 7.2 verbatim); verify `docs/start/installation.md` update is accurate; check that the refusal message for `sandbox update` on a dev install (§ 11) matches the spec's verbatim text including the migration URL.
- **Track 6 — Workarounds + deprecated patterns**: Grep `sandbox-cli/src/update/` and `sandbox-cli/src/cfg_migrations/` for: (a) any `unwrap()` or `expect()` in non-test paths (report each; the production paths should propagate errors via `?`); (b) any cosign sha256 constants duplicated outside `scripts/lib.sh` (both the Rust cosign-bootstrap and install.sh must source/reference the same values — flag if they diverge); (c) any `TODO`/`FIXME` introduced in M16 sessions; (d) any place where the `--apply-config-migration` root-check (§ 4.3) could be bypassed by calling an internal function directly (audit the call graph). For tests: flag any tautological test.

**Explicitly deferred.**

- Spec-delivery claim-to-code map → M16-S5.

**Exit criteria.**

- All six review tracks complete; findings collated into a single prioritized list.
- Every "must-fix" finding addressed, re-implemented where needed, and re-tested.
- Code coverage report generated; uncovered lines in security-critical paths (`apply_pending`, lock acquisition, `--apply-config-migration` root check, schema-mismatch refusal) have documented reasons or new tests.
- `cargo nextest run --workspace` default profile passes.
- `cargo nextest run --workspace --profile integration` passes.
- Lima E2E harness still passes after any fixes.

---

## M16-S5 — Spec 5 + five-spec arc delivery verification

**Entry criteria.** M16-S4 complete — all review findings addressed, all tests passing.

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` (all sections) + cross-references to Specs 1–4 for arc-level amendment verification.

**Rationale.** M16 is the terminal milestone for both Spec 5 and the entire five-spec arc. This session is therefore dual-purpose: it verifies that Spec 5 is fully delivered, and it confirms that every forward constraint Specs 1–4 placed on Spec 5 is honoured (the install-state file format Spec 4 produced is readable by `sandbox update --check`; the `migrate_v001` transform Spec 1 defined is invoked by the V001 adapter; the `GuestRequest::Version` primitive Spec 2 introduced is used in `--dry-run`'s stopped-session classification; Spec 3's systemd drop-in contract is respected by `§ 3.2.23`). It is the single place where cross-spec consistency is formally verified.

**In scope.**

- Claim-to-code map for Spec 5: every concrete claim in §§ 1–11 maps to (a) code locator (file path + line or function) + test locator, (b) an explicit out-of-scope bullet from Spec 5 § 12, or (c) a `progress` todo for future work. Format per `session-tracking` skill's `references/claim-to-code-format.md`.
- Arc-level amendment verification (Spec 5 depends on Specs 1–4; each forward constraint must have a code locator):
  - Spec 1 § 5 (`migrate_v001` pure transform) → V001 adapter at `cfg_migrations/v001_add_sandbox_to_allow_users.rs`.
  - Spec 2 § 3.10 (`GuestRequest::Version`) → stopped-session classification in `sandbox update --dry-run`.
  - Spec 3 § 4.3 (drop-in dir survives upgrade) → `§ 3.2.23` implementation never touches `.service.d/`.
  - Spec 3 § 8.6 (old images not auto-pruned) → `§ 3.2.20` implementation skips no `docker image rm`.
  - Spec 4 § 4.5 (install-state file schema) → `sandbox update --check` reads every field listed in § 4.5.
  - Spec 4 § 4.6 (install log format) → `sandbox update` appends with `sandbox-update` second token.
- Example replay:
  - Run `sandbox update --check --from <local-v1.1.0-tarball>` on a v1.0.0 install; verify exit code 3 and the output matches § 2.2 verbatim (status line, pending migrations, stopped-session count).
  - Run `sandbox update --dry-run --from <local-v1.1.0-tarball>` on the same install; verify the plan output matches § 2.3 shape (all 18 steps present with `would execute`/`skip`).
  - Run the full `sandbox update --from <local-v1.1.0-tarball> --yes`; verify daemon at v1.1.0, `sandbox doctor` green, backup set `completed_ok: true`, install state updated.
  - Run the rollback recipe verbatim from § 7.2 in a Lima VM; verify daemon back at v1.0.0, `sandbox doctor` green, lock file absent.
- Out-of-scope conformance: grep verification that Spec 5 § 12's list (automated `sandbox rollback`, automatic periodic lite image rebuild, CHANGELOG display, telemetry, cross-machine orchestration, `sandbox update --downgrade`, DB schema downgrade, pre-flight `--pre-flight` flag, `down` migrations) is absent from newly-added code.
- Spec 5 § 3.3 preserved-artefacts table: confirm each artefact listed as "preserved untouched" is genuinely absent from the stateful-step code paths (grep-level audit).
- Known-gap reconciliation: all `progress` todos for Spec 5 named follow-ons (automated rollback, CHANGELOG, cosign-pin maintenance, `test_update_skipping_major_version` placeholder).
- Deliverable written to:
  - `.tasks/specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-delivery.md`

**Explicitly deferred.**

- Nothing — this session closes Spec 5 and the full five-spec arc.

**Exit criteria.** Conjunctive — ALL must hold before M16-S5 is marked complete:

- Delivery file exists and every BLOCKER-tagged item in the claim-to-code map is resolved.
- Every concrete claim in Spec 5 §§ 1–11 has a code locator, an out-of-scope citation, or a named follow-on tracking reference.
- All arc-level forward-constraint amendments have code locators.
- Example replays complete without deviation from spec (output tokens exact, exit codes exact, backup-set manifest `completed_ok: true`, rollback produces clean `sandbox doctor` at prior version).
- Out-of-scope items absent from newly-added code (grep for each item in Spec 5 § 12).
- Spec 5 § 3.3 preserved-artefacts table: each artefact confirmed absent from stateful-step code paths.
- `cargo nextest run --workspace` default profile clean.
- `cargo nextest run --workspace --profile integration` clean.
- Lima E2E harness passes (all 11+ `test_update_*.py` cases).
- Code review of the delivery artifact approved.

---

## M16-S6 — Container guest-binary bind-mount (Spec 2 § 3.8.1 amendment)

**Entry criteria.** M16-S5 complete — five-spec arc verification gate has passed; deferred todos #141, #142, #147, #154 are open and queued for resolution.

**Spec reference.** `../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md` — § 3.6 (embedded guest binary — `include_bytes!` swap completes here), § 3.8.1 (container backend refresh mechanics — superseded by this session's bind-mount design), § 3.8 (`SessionRuntime::refresh_guest_binary` trait method, container arm only), § 9.4 (`docker cp`-on-`--read-only` risk note — premise invalidated, amended in lockstep with § 3.8.1); cross-reference Spec 3 § 5.4 (state-dir layout — adds `guest/` subdir to `ensure_base_dir_layout`).

**Selected todos.** This session opens by `progress todo select`-ing the four deferred items that all queued behind the `docker cp` / read-only-rootfs gap surfaced at M13-S5. Each is `progress todo resolve`-d at session close with this session's commit hash.

- **#141** (M13-S5) — Resolve Spec 2 § 3.8.1 `docker cp` gap; original suggestion was a lite Dockerfile `VOLUME` directive. Closed by the bind-mount design (a third path the todo did not enumerate; supersedes both option (a) `VOLUME` and option (b) carve-out).
- **#142** (M13-S5) — Switch `stage_embedded_guest_binary` to compile-time `include_bytes!` per Spec 2 § 3.6 option A. Closed by the new `build.rs` artefact + daemon-startup staging step.
- **#147** (M13-S6) — M14 production gap (#141 surrogate): lite-image lacks `VOLUME ['/usr/local/bin']` declaration. Closed by the bind-mount design which avoids needing `docker cp` against the rootfs entirely.
- **#154** (M13-S7) — Swap `stage_embedded_guest_binary` from sibling-file read to `include_bytes!` per Spec 2 § 3.6 option A (Spec 4 forward note). Closed by the same `include_bytes!` artefact #142 references.

**Rationale.** Spec 2 § 3.8.1's `docker cp` refresh mechanism is empirically broken on modern Docker (29.4+ with containerd snapshotter) against `--read-only` lite-mode containers — the production lite image has no `VOLUME` carve-out and dockerd rejects writes into the rootfs. Deferred todos #141, #142, #147, #154 all queued behind this single design decision. The bind-mount alternative is materially simpler than carving a `VOLUME` inside the rootfs: the daemon stages `sandbox-guest` once into `/var/lib/sandbox/guest/sandbox-guest` (mode `0755`, owner `sandbox:sandbox`) and bind-mounts that path read-only into each container at `/usr/local/bin/sandbox-guest`. Refresh becomes `docker restart` — running guests keep their open fd until next exec, new starts see the staged binary, the RO rootfs is untouched. One inode is shared across every live session, so the kernel page cache is shared too. Lima is untouched — its `systemctl restart sandbox-guest` model fits the VM substrate's ergonomics and there is no equivalent gain from changing it.

**In scope.**

- Amend Spec 2 § 3.8.1 (container backend refresh mechanics) and § 9.4 (`docker cp`-on-`--read-only` risk note) to record the bind-mount design as the v1 implementation choice; the original `docker cp` prose is preserved verbatim as historical context with a header pointing at this session's commit. Spec 2 § 3.6's option A becomes the realized embedding mechanism — staged once into the daemon's state-dir at startup, not staged per-refresh.
- Implement the amended Spec 2 § 3.8.1: daemon staging of the embedded guest binary at startup (one-shot, idempotent — sha256 compare against existing on-disk file), container backend `create_session` wires the staged path as a read-only bind-mount, container backend `refresh_guest_binary` becomes `docker restart` against the now-current bind-mount source. Resolves deferred todos #141/#147 (lite-image VOLUME workaround), #142/#154 (include_bytes! swap).
- Extend Spec 3 § 5.4's `ensure_base_dir_layout` with the `guest/` subdir the staging step requires; existing 0700-subdir / 0755-executable pattern applies.
- Spec 2 § 3.8 trait shape is unchanged. Spec 2 § 3.8.2 (Lima refresh mechanics) is unchanged per its substrate-ergonomics note — Lima's `systemctl restart sandbox-guest` model is unaffected.
- Migration of live sessions created on the previous container shape relies on Spec 2 § 3.4's existing compat gate — no new migration code. Legacy containers have `sandbox-guest` baked into their image layer at `/usr/local/bin/sandbox-guest`; on restart the bind-mount overlays this path with the staged binary, and the compat gate either short-circuits via `is_protocol_compatible` or routes through the new restart-based refresh.
- Update the Spec 2 delivery map (`api-session-isolation-guest-compat-design-delivery.md`) — P3.20 and P8.4 locators repointed at the amended § 3.8.1 implementation; rewrite the `integration_guest_refresh_container_backend` test against the production-shape `--read-only` lite image (the same image that ships from `make lite-image`) and update the test locator.

**Unit tests.**

- `stage_guest_binary_writes_embedded_bytes_when_path_absent`
- `stage_guest_binary_skips_when_sha256_matches`
- `stage_guest_binary_rewrites_when_sha256_differs_atomically`
- `container_run_argv_contains_guest_bind_mount` (asserts the `-v` flag on the constructed argv)
- `refresh_guest_binary_container_invokes_restart_not_cp` (mocked `Command` audit confirms no `docker cp` is invoked)

**Integration tests.**

- `integration_guest_refresh_container_backend` — **rewrite** to exercise restart-based refresh against a `--read-only` production-shape lite image; assert the in-container `/usr/local/bin/sandbox-guest` is bit-identical to the daemon's staged copy after restart and that `update_guest_versions` is called.
- `integration_guest_binary_swap_picked_up_by_new_sessions` — change the host-side staged file's bytes, create a new container session, verify the new session sees the new bytes.
- `integration_guest_binary_shared_inode_across_sessions` — start two container sessions; verify (via `/proc/<pid>/maps` or `stat`) they share the same backing inode for the bind-mounted binary, pinning the page-cache-sharing motivation.

**Explicitly deferred.**

- Lima container-shape unification — Lima sessions retain `systemctl restart sandbox-guest` per substrate ergonomics; no plan to change.
- `sandbox doctor` C-check for staged-binary sha256 drift — daemon staging is idempotent on every startup so drift auto-corrects; revisit only if a real flake surfaces.

**Exit criteria.**

- All unit tests in scope pass under the default nextest profile.
- All three integration tests pass under the `integration` nextest profile against the production-shape `--read-only` lite image (the same image that ships from `make lite-image`).
- `integration_guest_refresh_container_backend` no longer relies on a non-production fixture.
- Deferred todos #141, #142, #147, #154 marked resolved with this session's commit hash.
- Spec 2 § 3.8.1 amendment note added; Spec 2 delivery map (`.tasks/specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-delivery.md`) updated: P3.20 and P8.4 locators repointed at the bind-mount implementation and test locators updated to the rewritten integration test.
- `cargo nextest run --workspace` default and integration profiles clean; `cargo clippy --workspace` clean; code review approved.

---

## M16-S7 — Post-arc code fixes

**Entry criteria.** M16-S6 complete — the bind-mount design has landed so this session does not race with container-backend file edits.

**Spec reference.** `../specs/2026-05-11-daemon-productionization-design/2026-05-11-daemon-productionization-design-spec.md` — § 7.3 (CLI version handshake), § 12.2 (dev-mode signal), § 13.2 (`/diagnostics` wire shape); `../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md` — § 4.6 (install log destination); `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — § 2.3 (`--dry-run` output), § 5 (backup mechanics); cross-reference `.tasks/handoffs/m16-s4-triage-plan.md` (code-themed MAY-FIX subset).

**Selected todos.** This session opens by `progress todo select`-ing the six deferred items below plus the code-themed subset of the M16-S4 MAY-FIX triage plan. Each is `progress todo resolve`-d at session close with this session's commit hash.

- **#155** (M14-S2) — `/version` handshake symmetry on side-channel CLI paths. Closed by extending the version-check guard out of `send_request` into the helper shared by ssh/cp/sync/logs/events/inspect/describe.
- **#156** (M14-S4) — doctor C5/C10 dev-mode signal disambiguation. Closed by plumbing the M15 install-state file as the canonical dev-vs-prod signal into C5/C10 hint logic.
- **#158** (M14-S4) — `/diagnostics` `probe_failed` wire variant. Closed by adding a `probe_failed` variant to the diagnostics wire shape and matching CLI formatter so operators distinguish "docker daemon down" from "image missing."
- **#161** (M15-S2) — `$SANDBOXD_INSTALL_LOG` env override. Closed by reading the env var from install.sh and `sandbox update` log paths (falls back to the hardcoded path).
- **#167** (M16-S3) — WAL-safe sessions.db backup. Closed by extending the backup_sessions_db step to include `sessions.db-wal` and `sessions.db-shm` alongside `sessions.db`.
- **#176** (M16-S5) — `sandbox update --dry-run` skip-on-match render. Closed by wiring the per-step idempotency check into the renderer and labeling steps as `would execute` vs `would skip`.

**Rationale.** Six small operator- or test-author-visible improvements surfaced by M14/M15/M16 reviews, none of which justifies its own session. The set shares one reviewer hold-question ("does each fix close its named gap without touching adjacent behavior?") and avoids the noise of mixing code, test, and doc churn in one PR. The code-themed MAY-FIX subset from M16-S4's triage plan rides along under the same review lens.

**In scope.**

- `/version` handshake symmetry on side-channel CLI paths per Spec 3 § 7.3 — extend the version-check guard from `send_request` to the helper that ssh/cp/sync/logs/events/inspect/describe share.
- doctor C5/C10 dev-mode signal per Spec 3 § 12.2 — use the M15 install-state file (`/var/lib/sandbox/.install-state.json` per Spec 4 § 4.5) as the canonical dev-vs-prod signal in the C5/C10 hint logic.
- `/diagnostics` `probe_failed` wire variant per Spec 3 § 13.2 — add `probe_failed` to the diagnostics wire shape and matching CLI formatter.
- Install log destination env override per Spec 4 § 4.6 — `SANDBOXD_INSTALL_LOG` env var read by install.sh and `sandbox update` log paths.
- WAL-safe `sessions.db` backup per Spec 5 § 5 — extend the backup_sessions_db step in `update/mod.rs` to bundle `sessions.db` + `sessions.db-wal` + `sessions.db-shm`. SQLite restores cleanly from the bundle without checkpoint orchestration.
- `--dry-run` accuracy per Spec 5 § 2.3 — invoke the per-step idempotency check from the renderer; label each step `would execute` or `would skip` based on pre-applied state.
- Code-themed MAY-FIX items per `.tasks/handoffs/m16-s4-triage-plan.md` MF1–MF44 — implementer reads the triage plan and lands items whose nature is code quality (e.g. `log_step` gaps, `Path::to_str` unwrap audit, dev-mode 4-criteria classification, downgrade-refuse behavior).

**Explicitly deferred.**

- Test-themed MAY-FIX items → M16-S8.
- Docs-themed MAY-FIX items → M16-S9.

**Exit criteria.**

- All six explicit todos shipped and verified via existing or new unit/integration tests.
- WAL-safe backup verified via a unit test that backs up a `.db`+`-wal`+`-shm` bundle written by a daemon killed mid-write and asserts the restored DB contains the most recent committed transaction.
- `sandbox update --dry-run` against an up-to-date installation labels every applicable step `would skip`.
- Code-themed MAY-FIX items addressed; each closed MF number appears in the commit message.
- `cargo nextest run --workspace` default and integration profiles clean; `cargo clippy --workspace` clean; code review approved.

---

## M16-S8 — Post-arc test fixes

**Entry criteria.** M16-S6 complete — the rewritten `integration_guest_refresh_container_backend` has landed so this session does not race with that test's restructure.

**Spec reference.** `../specs/2026-05-11-helper-identity-assertion-design/2026-05-11-helper-identity-assertion-design-spec.md` — § 8.4 (helper integration tests), § 3.5 / P8.20 (audit-log shape); `../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md` — § 7.5 (integration test catalogue), § 9.2 (peercred-connector harness); `../specs/2026-05-11-daemon-productionization-design/2026-05-11-daemon-productionization-design-spec.md` — § 11.6 (integration test catalogue, HTTP-level missing-image refusal); `../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md` — § 6 (Lima E2E harness extension), § 7 amendment (local Sigstore stack replaces the cosign patch); cross-reference `.tasks/handoffs/m16-s4-triage-plan.md` (test-themed MAY-FIX subset).

**Selected todos.** This session opens by `progress todo select`-ing the nine deferred items below plus the test-themed subset of the M16-S4 MAY-FIX triage plan. Each is `progress todo resolve`-d at session close with this session's commit hash.

- **#148** (M13-S7) — `integration_route_helper_uid_without_passwd_denies_cleanly` (Spec 1 § 8.4). Closed by the Lima harness + peercred-connector landing in this session.
- **#149** (M13-S7) — allow-path audit-log-write-failure integration test (Spec 1 § 3.5 / P8.20). Closed alongside #148 in the same Lima harness pass.
- **#150** (M13-S7) — `integration_owner_isolation_uid_without_passwd_closes_connection` (Spec 2 § 7.5 / § 4.1). Closed by peercred-connector + Lima multi-uid coverage.
- **#151** (M13-S7) — `integration_session_isolation_404_on_foreign_id` (Spec 2 § 7.5 / § 9.2). Same closure path.
- **#152** (M13-S7) — `integration_guest_refresh_lima_backend`. Closed by adding the Lima backend test gated on `/dev/kvm` following the `lima_integration.rs` cfg-attr pattern.
- **#153** (M13-S7) — peercred-connector setuid helper + Lima harness provisioning (Spec 2 § 9.2). The enabling infrastructure for #148-#152; lands first inside this session.
- **#159** (M14-S4) — HTTP-level test for refused-on-missing-gateway-image (Spec 3 § 11.6). Closed by a thin integration test that exercises POST /sessions's refusal body.
- **#164 + #172 (consolidated)** (M15-S3 + M16-S4) — local Sigstore stack via `sigstore/scaffolding`. Closed by standing up Fulcio + Rekor + dex via docker-compose in `tests/install-e2e/conftest.py` at session scope and removing the `cosign_bootstrap` + `sigstore_verify` patches. install.sh runs unchanged; the real verify-blob code path is exercised in the install E2E harness for the first time.
- **#168** (M16-S3) — `release_tarball_x86_64` fixture cache invalidation. Closed by mirroring the `release_tarball_x86_64_bumped` mtime check (`conftest.py:441`).

**Rationale.** This is the largest of the three fix-up sessions because two of its items (peercred-connector and the Sigstore stack) are real harness infrastructure, not just additional tests. The peercred-connector helper alone unlocks six Lima multi-uid tests that were gated on it (#148–#152 + the helper itself #153), so bundling keeps the harness change coherent and avoids landing the helper without the tests it enables. The Sigstore stack closes the long-standing gap that install E2E never exercised the real cosign verification path. Sizing watch: if review burden exceeds one PR's comfortable surface, splitting at the Lima/Sigstore boundary is a clean cut.

**In scope.**

- Lima multi-uid test infrastructure per Spec 2 § 9.2 — build the `peercred-connector` setuid-root helper that drops to a target uid and forwards a daemon request from stdin to the unix socket; provision into the Lima E2E test VM template via `install -m 4755 /usr/local/lib/sandboxd-tests/peercred-connector`.
- Six Lima multi-uid tests per Spec 1 § 8.4 and Spec 2 § 7.5 — implement the five named tests above + the audit-log allow-path failure test per Spec 1 § 3.5 / P8.20.
- HTTP-level missing-gateway-image test per Spec 3 § 11.6 — thin integration test that asserts POST /sessions returns the spec-pinned 4xx body, complementing the primitive-level test already shipped.
- Local Sigstore stack per Spec 4 § 7 (amended) — stand up Fulcio + Rekor + dex via `sigstore/scaffolding`'s docker-compose recipe in `tests/install-e2e/conftest.py` at session scope; remove `cosign_bootstrap` + `sigstore_verify` patches; install.sh runs `verify-blob` unchanged with cert-identity-regexp + OIDC-issuer matching the local test identity.
- Fixture cache invalidation in `tests/install-e2e/conftest.py` — mirror the `release_tarball_x86_64_bumped` mtime check (`conftest.py:441`) into `release_tarball_x86_64` so workspace `.rs` edits invalidate the cached tarball.
- Test-themed MAY-FIX items per `.tasks/handoffs/m16-s4-triage-plan.md` MF1–MF44 — implementer reads the triage plan and lands items whose nature is test coverage (per-test coverage gaps, additional assertions).

**Explicitly deferred.**

- Code-themed MAY-FIX items → M16-S7.
- Docs-themed MAY-FIX items (including `docs/operate/update.md` authorship) → M16-S9.

**Exit criteria.**

- All six Lima multi-uid tests pass under the `integration` nextest profile inside the Lima E2E VM harness.
- `peercred-connector` builds, installs setuid-root inside the VM, and successfully exercises both `TARGET_UID_OPERATOR` (`$(id -u agent)`) and `TARGET_UID_DAEMON` (`$(id -u sandbox)`) call paths.
- Install E2E suite (`tests/install-e2e/test_install_*.py` + `test_update_*.py`) passes with the local Sigstore stack — at least one test asserts the real `cosign verify-blob` ran (e.g. install log carries the unstubbed step lines), and a negative test asserts install.sh aborts cleanly when the test tarball's signature is missing or invalid.
- `release_tarball_x86_64` fixture mtime-invalidates after a workspace `.rs` edit, verified by an explicit pytest case.
- Test-themed MAY-FIX items addressed; each closed MF number appears in the commit message.
- `cargo nextest run --workspace` default and integration profiles clean; `pytest tests/install-e2e` clean; code review approved.

---

## M16-S9 — Post-arc docs sweep

**Entry criteria.** M16-S6, M16-S7, M16-S8 complete — doc updates reflect final shipped behavior across the bind-mount, code, and test changes.

**Spec reference.** `../specs/2026-05-11-helper-identity-assertion-design/2026-05-11-helper-identity-assertion-design-spec.md` — § 3.5 (audit log destination + shape); `../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md` — § 2.2 (API isolation rule), § 5 (wire snapshot — `owner_username` + 404 shape); `../specs/2026-05-11-daemon-productionization-design/2026-05-11-daemon-productionization-design-spec.md` — § 6 (`sandbox doctor` surface), § 12 (dev-mode); `../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md` — § 4.2 (install invocation patterns); cross-reference `.tasks/handoffs/m16-s4-triage-plan.md` (docs-themed MAY-FIX subset, including `docs/operate/update.md` authorship).

**Selected todos.** This session opens by `progress todo select`-ing the five deferred items below plus the docs-themed subset of the M16-S4 MAY-FIX triage plan. Each is `progress todo resolve`-d at session close with this session's commit hash.

- **#144** (M13-S6) — `docs/reference/http-api.md` per-endpoint audit. Closed by documenting per-caller filtering, SO_PEERCRED-derived identity, 404-on-foreign-id, `owner_username` field per Spec 2 § 5.
- **#145** (M13-S6) — `docs/reference/cli.md` ownership notes. Closed by mentioning session ownership in `sandbox ps` and adding a global flags/credentials preamble.
- **#146** (M13-S6) — `docs/guides/troubleshooting.md` route-helper deny recipe. Closed by adding the recipe with the audit log path per Spec 1 § 3.5.
- **#165** (M15-S3) — install.sh `--help` mentions `--from` requires `--version` per Spec 4 § 4.2.
- **#169** (M16-S3) — sandbox-user nologin landmine docs for test authors. Closed by a one-paragraph note in `tests/install-e2e/README.md`.

**Rationale.** Five narrow doc edits across user-facing operator/developer surfaces (HTTP API reference, CLI reference, troubleshooting guide, install.sh `--help`, test-author README). Bundling into one author pass means consistent style and cross-references. Lands after S7/S8 so the docs reflect actually-shipped behavior, not aspirational state. The docs-themed MAY-FIX subset rides along — `docs/operate/update.md` authorship is the largest of these.

**In scope.**

- HTTP API per-endpoint audit at `docs/reference/http-api.md` — for each endpoint enumerated in Spec 2 § 2.3 (H1-H12), document the per-caller filtering behavior, SO_PEERCRED-derived identity, 404-on-foreign-id wire shape per Spec 2 § 5, and the `owner_username` field on session response bodies.
- CLI reference at `docs/reference/cli.md` — mention session ownership in the `sandbox ps` description per Spec 2 § 2.2; add a global flags/credentials preamble explaining caller-scoped views.
- Troubleshooting guide at `docs/guides/troubleshooting.md` — add a route-helper deny recipe pointing operators at the audit log path per Spec 1 § 3.5 (production path `/run/user/<uid>/sandboxd/route-helper-audit.log` with dev fallbacks).
- install.sh `--help` text — note that `--from` requires `--version` per Spec 4 § 4.2 (`resolve_target_version` doesn't infer from the tarball filename).
- Test-author README at `tests/install-e2e/README.md` — one-paragraph note about the sandbox-user nologin landmine: `sudo -u sandbox <direct-cmd>` works for direct binaries (jq, tee, cat, env, install), but shell-invoking forms (`flock -c`, `sh -c`, `bash -c`) fail with "This account is currently not available" because the user's shell is `/usr/sbin/nologin`.
- Docs-themed MAY-FIX items per `.tasks/handoffs/m16-s4-triage-plan.md` MF1–MF44 — including `docs/operate/update.md` authorship per Spec 5 § 7's rollback recipe and the M16-S3 stateful flow.

**Explicitly deferred.**

- Code-themed MAY-FIX items → M16-S7.
- Test-themed MAY-FIX items → M16-S8.

**Exit criteria.**

- All five explicit doc files updated.
- `docs/operate/update.md` authored covering the M16-S3 stateful flow + the documented rollback recipe.
- Docs-themed MAY-FIX items addressed; each closed MF number appears in the commit message.
- Markdown lint clean; cross-references resolve; code review approved.

---

## M16-S10 — Lima multi-uid harness for peercred + isolation tests

**Entry criteria.** M16-S8 complete — the `peercred-connector` setuid-root binary scaffolded at M16-S8 exists in the workspace; the install-e2e Lima VM template already provisions the test fixtures the harness depends on; deferred todos #148, #150, #151 are open and queued for resolution under their renumbered tracking.

**Spec reference.** `../specs/2026-05-11-helper-identity-assertion-design/2026-05-11-helper-identity-assertion-design-spec.md` — § 8.4 (helper integration tests, multi-uid coverage); `../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md` — § 7.5 (per-caller API session ownership integration tests), § 9.2 (peercred-connector harness contract).

**Selected todos.** Three deferred items, all surfaced during M16-S8 when peercred-connector landed without the Lima VM wiring needed to actually invoke it as a non-root non-`sandbox` operator.

- **#148** — `integration_route_helper_uid_without_passwd_denies_cleanly` (Spec 1 § 8.4). Closed PASS.
- **#150** — daemon closes connection cleanly when peer's uid is absent from `users.conf` (Spec 2 § 7.5). Closed PASS — the daemon emits RST or EOF depending on the kernel's connection-shutdown timing, so the test accepts either signal.
- **#151** — `integration_session_isolation_404_on_foreign_id` deferred to a future session (renumbered as todo #185). Probe work in this session uncovered a real daemon-side bug behind the test (a foreign-id lookup that returns the wrong status code on the first cross-operator request); the surface fixes attempted here (sqlite3 provisioned inline, synthetic-row SQL piped through heredoc) became dead-end progress once the deeper bug was diagnosed, and the test is queued for a fix-and-cover session against the daemon.

**Rationale.** The peercred-connector setuid helper was scaffolded at M16-S8 but never wired into a Lima VM, so the multi-uid tests it enables (#148, #150, #151) could not run against a real installation. This session stood the harness up: dynamic invoking-user discovery so the tests run regardless of the operator's uid on the test host, a libexec-resolution fix-up arc that unblocked a separate M16-S6 regression encountered along the way (sandbox-guest read from disk by the daemon but never shipped by the install pipeline), and finally the actual test bodies for #148 and #150. The #151 surface attempts (sqlite3 inline + JSON heredoc for synthetic foreign-session rows) shipped as forward progress before the deeper bug was diagnosed, but the test itself stays deferred until the daemon is fixed.

**In scope.**

- Lima VM provisioning of `peercred-connector` — fixture wires the M16-S8-built setuid-root helper into the install-e2e VM template so tests can drop to any uid the harness chooses.
- Dynamic invoking-user discovery in the peercred isolation tests — replace hardcoded `agent` references with a runtime `id`-based lookup so the suite passes on any host where the developer's login is not `agent`.
- sandbox-guest install-path regression fix (surfaced during this session) — the M16-S6 `stage_embedded_guest_binary` code reads sandbox-guest from disk at daemon startup, but the install pipeline (install.sh + release tarball CI) never shipped the binary; fixed by shipping `sandbox-guest` to `/usr/local/libexec/sandboxd/` and teaching the daemon's resolver to look env-override → libexec → cargo-target-fallback. The post-install assertion test confirms the binary lands under libexec.
- `integration_route_helper_uid_without_passwd_denies_cleanly` (#148) — driven through peercred-connector against the production-shape route helper; asserts a clean deny with the spec-pinned audit-log line.
- `integration_owner_isolation_uid_without_passwd_closes_connection` (#150) — peercred-connector calls the daemon as a uid not in `users.conf`; assertion accepts RST or EOF as the daemon's close-without-response signal.
- #151 surface progress — sqlite3 provisioned inline (the Lima VM lacked it by default), synthetic-row SQL piped through heredoc rather than escaped `"..."` string. These ship as forward groundwork; the test itself stays deferred behind todo #185.

**Integration tests.**

- `integration_route_helper_uid_without_passwd_denies_cleanly` — PASS.
- `integration_owner_isolation_uid_without_passwd_closes_connection` — PASS.
- `integration_session_isolation_404_on_foreign_id` — deferred (todo #185).
- Post-install assertion: `sandbox-guest` lands under `/usr/local/libexec/sandboxd/` after a fresh install.

**Explicitly deferred.**

- `integration_session_isolation_404_on_foreign_id` (#151 / renumbered #185) — pending the daemon-side cross-operator-404 bug-fix session.

**Exit criteria.**

- Tests #148 and #150 pass under the install-e2e suite in a clean Lima VM.
- `peercred-connector` runs correctly under the dynamically-discovered invoking-user and against the `sandbox` daemon uid on any host.
- The install pipeline ships `sandbox-guest` under `/usr/local/libexec/sandboxd/` and the daemon resolves it without env override on a default install.
- Closed todos (#148, #150) marked resolved with this session's commit hashes; #151 renumbered as #185 with a note pointing at the diagnosed daemon-side bug.

---

## M16-S11 — Local Sigstore stack via hand-rolled docker-compose + OIDC impersonation

**Entry criteria.** M16-S8 complete — the install-e2e suite still patches out `cosign_bootstrap` + `sigstore_verify` in install.sh; the M16-S8 attempt to consolidate todos #164 + #172 around `sigstore/scaffolding`'s docker-compose recipe is reopened after the first implementer surfaced architectural blockers.

**Spec reference.** `../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md` — § 7 (amended): the local Sigstore stack replaces the original cosign-patch approach; install.sh must exercise the real `cosign verify-blob` path against a local trust chain rather than running against a stubbed binary.

**Rationale.** The original framing in M16-S8 was "stand up `sigstore/scaffolding`'s docker-compose recipe and impersonate the production OIDC issuer." Two architectural blockers surfaced during the first implementation attempt: `sigstore/scaffolding` is Kubernetes-only with no docker-compose recipe (the source-tree manifests target Helm/Kustomize), and cosign's literal-string OIDC-issuer match forces DNS resolution + TLS + CA injection at the host level — there is no override for issuer comparison. The session reset around a hand-rolled stack: one local CA, six containers wired together by Docker network alias under the production issuer hostname, and a static JWKS served by nginx with Python `pyjwt` minting test tokens (replacing dex entirely). Splitting this into its own session is justified by two architectural decisions that drove design work measured in days, not hours: the no-TUF-root choice (cosign called with `--certificate-chain` plus env-var public keys rather than a synthesized TUF repo) and the network-alias-for-production-hostname choice (Docker network alias rather than DNS-server-in-the-loop).

**In scope.**

- Local CA + per-service TLS certs — `init.sh` generates a single root CA and issues certs for Fulcio, Rekor, Tesseract CT, and the OIDC nginx, all valid for the production issuer hostname so cosign's literal-string match succeeds.
- `docker-compose` for the six-container stack — Fulcio (code-signing CA), Rekor (transparency log), Trillian (Rekor's storage backend), Tesseract CT (certificate transparency for Fulcio), nginx serving static JWKS + Python `pyjwt`-based token endpoint (replaces dex), MySQL backing Trillian.
- Production OIDC issuer impersonation — Docker network alias attaches the nginx container to the production hostname inside the compose network, so cosign's issuer-comparison resolves against the local stack with the production string.
- Fulcio configuration — local CA inlined as PEM in the config; no TUF root synthesized.
- Smoke test — `sign-blob` + `verify-blob` round-trip against the local stack; runs in ~36 seconds. Verifies the full chain (token mint → Fulcio cert issuance → Rekor entry → verify-blob success) without requiring `install.sh`.
- Tesseract CT image digest pinned (no floating tags in test infrastructure).
- Design notes — the full design document (1500+ lines) lives at `.tasks/handoffs/20260516-161203-m16-s11-sigstore-stack-design.md` (gitignored).

**Explicitly deferred.**

- Install-e2e integration of the stack → M16-S12 (this session ships the stack and the smoke test; wiring it into the install-e2e suite is a separate concern).
- TUF root synthesis — out of scope by design choice; cosign is invoked with `--certificate-chain` + env-var public keys.
- dex — replaced wholesale by the nginx-static-JWKS + pyjwt minter combination; no dex container ships.

**Exit criteria.**

- `docker compose up` brings the full six-container stack to ready state in ~30 seconds; tear-down in ~12 seconds.
- Smoke test (sign-blob + verify-blob round-trip) PASS in ~36 seconds.
- Cosign's literal-string OIDC-issuer match succeeds against the local stack.
- Design doc captured at `.tasks/handoffs/20260516-161203-m16-s11-sigstore-stack-design.md`.

---

## M16-S12 — Install-e2e integration of the local Sigstore stack

**Entry criteria.** M16-S11 complete — the local Sigstore stack stands up cleanly with its smoke test green.

**Spec reference.** `../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md` — § 7 (amended): install.sh exercises the real `cosign verify-blob` path; `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — § 3.1.10 (sigstore verify-blob during `sandbox update`).

**Selected todos.**

- **#164** (M15-S3) — install-e2e stubs out `cosign_bootstrap` + `sigstore_verify` in install.sh. Closed.
- **#172** (M16-S4) — install-e2e never exercises the real verify-blob code path. Closed by the same wiring.

**Rationale.** With the local Sigstore stack standing up in M16-S11, this session wires it into the install-e2e suite. The plumbing is split across install.sh and the update path's `verify_signature` so both surfaces exercise the real cosign code path; tarballs are signed against the local stack and Lima VMs are taught to resolve the production issuer hostname into the stack via `/etc/hosts` injection. The session was originally framed as a straight wiring task but acquired a tail bug-fix arc once the cached-tarball + Rekor-key-churn interaction surfaced. The root-cause fix (re-sign the cached tarball when Rekor's signing key changes) keeps the fixture layer authoritative rather than papering over the stale-key error in tests.

**In scope.**

- `SANDBOX_INSTALL_TEST_*` env-var wiring in install.sh — `sigstore_verify` reads test-env overrides for the trust-chain inputs so install.sh runs unchanged in production but resolves against the local stack in tests.
- `SANDBOX_UPDATE_TEST_*` env-var wiring in `update/fetch.rs::verify_signature` — parallel to the install.sh path so `sandbox update` exercises the same verify-blob code path against the same local stack.
- Session-scope `sigstore_stack` conftest fixture — brings the stack up once per pytest session and tears it down at exit; minimal wall-clock overhead amortized across the suite.
- `build-local-tarball.sh` signs against the local stack — fixture-built tarballs come with valid `.sigstore` bundles minted by the local Fulcio + Rekor pair.
- Lima VM `/etc/hosts` injection — the production OIDC issuer hostname (`token.actions.githubusercontent.com`) is resolved into the Docker stack from inside the VM so cosign's verify-blob resolves correctly.
- Drop the `cosign_bootstrap` + `sigstore_verify` patches that previously stubbed install.sh — install.sh now runs untouched in the install-e2e suite.
- Negative coverage — `test_install_happy_path` complemented by tests that assert install.sh aborts cleanly on (a) a tampered tarball and (b) a missing `.sigstore` bundle.
- POSIX-portable byte-flip via `/dev/zero` — replaces `printf '\x00'` in the tampered-tarball negative test for shell portability.
- Tampered-tarball delivery into the Lima VM — copy via `limactl cp` (with chmod-before-flip if needed) rather than the in-VM `dd` approach that hit EACCES under the test uid.
- `SANDBOX_INSTALL_TEST_DEBUG_COSIGN_STDERR` debug toggle — kept as durable test diagnostics for future cosign churn; gated to test env only so it cannot leak into production install paths.
- Rekor signing-key churn root-cause fix — Rekor's `--rekor_server.signer=memory` regenerates its signing key on every restart, so cached `.sigstore` bundles attached to fixture tarballs carry stale `logID` values that trip "rekor log public key not found for payload" on the next session's verify. The fix re-signs the cached tarball when the Rekor key churns rather than invalidating the entire fixture cache.
- Tesseract CT digest pin (carries forward from M16-S11).

**Integration tests.**

- `test_install_happy_path` — PASS in 216s against the local stack.
- Both negative tests (tampered tarball, missing `.sigstore`) — PASS in 345s combined.
- M16-S11 smoke test — still PASS.

**Explicitly deferred.**

- Nothing — the session closes #164 + #172 and clears the install-e2e Sigstore gap end-to-end.

**Exit criteria.**

- Install-e2e suite runs with the local Sigstore stack instead of patching cosign out; install.sh and `update/fetch.rs` both exercise the real `cosign verify-blob` code path.
- Negative tests assert install.sh aborts cleanly on tampered and missing artefacts.
- Cached fixture tarballs re-sign automatically when Rekor's in-memory signing key rotates between sessions; no stale-`logID` flakes.
- Closed todos #164 + #172 marked resolved with this session's commit hashes.
- `cargo nextest run --workspace` clean (1444/0/147); `cargo clippy --workspace` clean; install-e2e suite green.

---

## M16-S13 — Real correctness + security fixes

**Entry criteria.** M16-S12 complete — the Sigstore arc has closed, install-e2e exercises the real `cosign verify-blob` code path.

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — § 7.2 (rollback recipe); `../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md` — § 3.8.1 (container guest-binary bind-mount, amended); `../specs/2026-05-11-release-and-install-infrastructure-design/2026-05-11-release-and-install-infrastructure-design-spec.md` — § 7 (sigstore trust-root surface).

**Selected todos.**

- **#183** (M16-S9) — Rollback recipe restores only `sessions.db.bak`; the backup set includes `sessions.db-wal` + `sessions.db-shm` since M16-S7 commit `36530a9` but the rollback path drops them. Operators following the documented recipe lose in-flight transactions.
- **#190** (M16-S12) — Drop the daemon-side guest-binary staging step (M16-S6 vestige). With `sandbox-guest` shipping at `/usr/local/libexec/sandboxd/sandbox-guest` after M16-S10, the daemon's startup copy into `/var/lib/sandbox/guest/sandbox-guest` is redundant: container backend can bind-mount the libexec source directly. `sandbox update` already provides atomicity via libexec rename.
- **#192** (M16-S12) — Feature-gate `SANDBOX_INSTALL_TEST_*` + `SANDBOX_UPDATE_TEST_*` Sigstore env-var redirects behind a Cargo `test-env-override` feature (or sibling). Today the env reads at `sandbox-cli/src/update/fetch.rs:252-272` are unconditional, so a production-built sandbox can have its cosign trust root substituted at runtime.

**Rationale.** Three load-bearing fixes that surfaced during the M16-S6..S12 arc but didn't fit any closing session. Each carries real risk in production (data loss on rollback, an unnecessary daemon startup surface, and a runtime trust-root substitution hole), but each is small enough (1–3 commits) that bundling them yields one coherent "real bugs" session with a shared verification matrix (`cargo` + happy-path install-e2e). Splitting would triple the review and verification overhead for no scope benefit. Each fix has a different surface — docs/spec/test for #183, daemon code + Spec 2 amendment for #190, Rust `cfg`-gating + install.sh hardening for #192 — but all three want the same end-to-end install-e2e check.

**In scope.**

- #183 — `docs/guides/rollback.md` step 7 restores `.db` + `-wal` + `-shm`; Spec 5 § 7.2 example block sync; `test_update_then_manual_rollback` asserts all three files land via the rollback recipe.
- #190 — Remove `stage_guest_binary_at`, `STAGED_GUEST_FILE_RELPATH`/`STAGED_GUEST_SUBDIR` constants, the `guest/` subdir branch in `ensure_base_dir_layout`, and the daemon-startup staging call (all in `sandbox-core` + `sandboxd`). Container backend's bind-mount source switches to `guest_agent_path()` output directly. Update Spec 2 § 3.8.1 amendment to reflect the simpler design; the original `include_bytes!` framing was abandoned in M16-S6 and the staging step was kept as an artefact of that plan.
- #192 — Gate the Rust env-var read block in `sandbox-cli/src/update/fetch.rs::verify_signature` under `#[cfg(feature = "test-env-override")]` (existing feature on `sandbox-route-helper` provides the pattern). Enable the feature in the install-e2e harness build. Verify a `--release` build no longer carries the `SANDBOX_*_TEST_*` env-var names as strings (`grep` over the resulting binary). install.sh shell side: evaluate whether to leave-as-is (shell scripts can't be feature-gated), hardcode-prod, or add a build-time substitution; pick the path that minimises production-surface risk.

**Explicitly deferred.**

- Daemon-side cross-operator peercred bug → M16-S14 (substantive enough to warrant its own session).
- Test-flake hardening (#178, #187, #188) → M16-S15.

**Exit criteria.**

- `cargo nextest run --workspace` default profile clean; `cargo nextest run --workspace --profile integration` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `test_install_happy_path[ubuntu-22.04]` + `test_update_then_manual_rollback` + `test_install_sigstore_refusals` all green under the install-e2e harness.
- Release-built binary contains no `SANDBOX_*_TEST_*` env-var name strings; debug-built (with `test-env-override` feature) still honours them.
- Spec 2 § 3.8.1 amendment reflects the simpler bind-mount-direct-from-libexec design; staging step removed from code and from the spec amendment.
- Spec 5 § 7.2 example block byte-for-byte matches `docs/guides/rollback.md`.
- Closed todos #183, #190, #192 marked resolved with this session's commit hashes.

---

## M16-S14 — Daemon-side cross-operator peercred bug

**Entry criteria.** M16-S13 complete — code-side correctness/security fixes have landed and won't cascade-fail the verifier.

**Spec reference.** `../specs/2026-05-11-api-session-isolation-guest-compat-design/2026-05-11-api-session-isolation-guest-compat-design-spec.md` — § 4.1 (close-on-no-resolve), § 7.5 (cross-operator wire shape).

**Selected todos.**

- **#185** (M16-S10) — Daemon-side cross-operator peercred bug. Bob's request via `peercred-connector` returns FIN+0bytes; `sudo -u bob curl --unix-socket ...` against the same endpoint returns the expected 404; `sudo -u bob sandbox ps` fails "operation was canceled" on the 2nd request of a keep-alive connection. Alice's identical CLI invocations work. Daemon journal silent.
- **#151** (M13-S7) — `integration_session_isolation_404_on_foreign_id` — install-e2e Lima multi-uid test exists (M16-S10 commit `92677e2`) but its assertion path can't run until #185 is fixed.

**Rationale.** Real production-relevant bug that surfaced during M16-S10 verification: multi-operator workflows are partially broken today. The diagnosis handoff at `.tasks/handoffs/20260516-153431-subagent-151-daemon-side-bob-request-bug.md` catalogues three competing hypotheses — HTTP keep-alive interaction with the peercred listener, hyper rejecting `Connection: close` + half-close, silent uid-resolution failure logged elsewhere — with reproduction shell scripts for each. Investigation is substantive enough to warrant its own session: the fix surface is unknown until the diagnosis lands, and the verification cycle (peercred-connector + curl + CLI all against the same daemon, under two operator uids) costs a full Lima boot per iteration. #151 closes naturally once #185's fix lands; bundling them keeps the verification path coherent.

**In scope.**

- Diagnose the bug per the three hypotheses in the handoff file. Add daemon-side tracing if needed to surface the silent close path.
- Land the daemon-side fix (location unknown until diagnosis — likely in the peercred listener at `sandboxd/sandboxd/src/main.rs` around the SO_PEERCRED uid-resolution path, or in the axum/hyper keep-alive handling for the Unix socket listener).
- Re-run `test_install_isolation_404_on_foreign_id` (#151) under the install-e2e harness; assert it passes against its actual contract (404 for foreign session id, not the FIN+0bytes the harness sees today).
- Re-run `test_install_owner_isolation_uid_without_passwd_closes_connection` (#150) to confirm the fix didn't regress it.

**Explicitly deferred.**

- Anything that turns the fix into a wider HTTP handler refactor — if the diagnosis surfaces a structural issue (e.g. the peercred listener wants its own axum router instance), surface that as a follow-on session rather than expanding this one.
- RST-vs-FIN spec clarification (#186 was dropped earlier; revisit only if the diagnosis turns out to hinge on it).

**Exit criteria.**

- `cargo nextest run --workspace` default + integration profiles clean.
- `test_install_owner_isolation_uid_without_passwd_closes_connection` (#150) still passes.
- `test_install_session_isolation_404_on_foreign_id` (#151) passes against its actual contract — exercises peercred-connector → daemon → 404 wire shape per Spec 2 § 7.5.
- The diagnosed daemon-side close path is covered by a regression test (location TBD by the fix shape).
- Closed todos #151 + #185 marked resolved with this session's commit hashes.

---

## M16-S15 — Test infrastructure hardening

**Entry criteria.** M16-S14 complete — daemon-side cross-operator bug fixed, so the test-flake verification matrix doesn't cascade-fail.

**Spec reference.** N/A — test-infra hygiene; no spec sections.

**Selected todos.**

- **#178** (M16-S6) — `integration_create_session_container_*` tests create real `sandbox-<sid>` Docker containers but are not in `nextest.toml`'s `docker-sandbox-namespace` test-group filter. Latent race against the group's orphan-reaper.
- **#187** (M16-S10) — Two `integration_guest_refresh` tests (`integration_guest_binary_shared_inode_across_sessions`, `integration_guest_binary_swap_picked_up_by_new_sessions`) are Docker-race flaky under parallel nextest; both pass on retry.
- **#188** (M16-S12) — Rekor's `--rekor_server.signer=memory` regenerates its signing key on every container restart. Cached `.sigstore` bundles trip "rekor log public key not found for payload" without the M16-S12 fixture-layer workaround.

**Rationale.** Test hygiene that travels together. #178 + #187 share the same fix surface (`sandboxd/.config/nextest.toml`'s `docker-sandbox-namespace` test-group filter) — both move into the existing group so the orphan-reaper serialises against them. #188 swaps Rekor's in-memory signer for a persistent on-disk signer in `tests/install-e2e/sigstore-stack/`, eliminating the bundle-staleness class of failures entirely (the M16-S12 fixture workaround stays but becomes redundant). Bundled into one session because all three are low-LOC config changes that share a verification gate (full `cargo nextest --profile integration` run). The Python install-e2e test-naming sweep rides along: the `integration_*` prefix on Python pytest functions is misleading (pytest discovers by `test_*`, not `integration_*`, and these tests don't participate in nextest's `integration` profile filter at all) — strip the prefix to match Python conventions.

**In scope.**

- #178 — extend `nextest.toml`'s `docker-sandbox-namespace` test-group filter to include `integration_create_session_container_*`. Grep audit for any other test family that creates `sandbox-<sid>` Docker containers without being in the group.
- #187 — add the two flake-prone `integration_guest_binary_*` tests to the same group filter (covered by the same extended filter pattern if scoped right).
- #188 — `init.sh` generates a persistent Rekor signing key alongside the existing trust material. `docker-compose.yml` swaps `--rekor_server.signer=memory` for `--rekor_server.signer=<path>` reading the generated key. The M16-S12 fixture-layer re-sign-on-mtime-change check stays as belt-and-suspenders.
- Python pytest test-naming sweep: rename the M16-S10 + M16-S11 Python tests that carry the `integration_*` prefix to `test_*` (matching Python conventions); audit `tests/install-e2e/*.py` and `tests/e2e/*.py` for any other `integration_*`-prefixed Python functions and rename them.

**Explicitly deferred.**

- Coverage gaps (#174, #180, #191) → M16-S16.
- Final docs sweep → M16-S17.

**Exit criteria.**

- `cargo nextest run --workspace --profile integration --features sandbox-route-helper/test-env-override` clean under parallel execution (no `-j 1` workaround needed for the `integration_guest_binary_*` or `integration_create_session_container_*` families).
- `docker compose up` for the local Sigstore stack survives a restart without re-signing fixture tarballs; `pytest tests/install-e2e/test_sigstore_stack_smoke.py` green.
- `grep -rn 'def integration_' tests/` returns zero matches; all Python tests follow `test_*` convention.
- Closed todos #178, #187, #188 marked resolved with this session's commit hashes.

---

## M16-S16 — Test coverage gaps + bookkeeping

**Entry criteria.** M16-S15 complete — test infrastructure is stable, so coverage additions land against a clean baseline.

**Spec reference.** `../specs/2026-05-11-update-infrastructure-design/2026-05-11-update-infrastructure-design-spec.md` — § 2.3 (per-session classification in `--dry-run` output) for #174; cfg_migrations source pattern in `sandbox-cli/src/cfg_migrations/mod.rs` for #180; `.tasks/handoffs/m16-s4-triage-plan.md` for #191.

**Selected todos.**

- **#174** (M16-S5) — `test_update_with_recreate_session_classification` missing from `tests/install-e2e/`. The runtime arm is covered by `integration_guest_refresh.rs` in `sandbox-core`. CLI surface is wired (M16-S4 `--dump-proto-version` + `classify_stopped_sessions`). Only the install-e2e end-to-end test that asserts `sandbox update --dry-run` emits per-session `recreate` classification is missing.
- **#180** (M16-S8) — MF27+MF28 — extend backup module test coverage with in-process tests for `apply_migration` and atomic-write semantics in `sandbox-cli/src/update/backup.rs`. Parallel to M16-S8 commit `dd58e36`'s `apply_migration_in_memory` triple for cfg_migrations.
- **#191** (M16-S12) — MF12 + MF39 from `.tasks/handoffs/m16-s4-triage-plan.md` are unaccounted for: not closed in any M16-S6..S12 commit message, not captured as deferred todos. Re-read each entry and decide close / mark stale / scope as own todo.

**Rationale.** Three coverage / bookkeeping items that fit one session. Adding test coverage is mechanically distinct from fixing flakes (M16-S15) but shares no source-tree overlap with anything else open. #174 follows the existing `test_update_multi_version.py` shape so it's mostly fixture work; #180 lifts the cfg_migrations test pattern to backup.rs (the "larger setup cost" mentioned at deferral time is a one-off temp-fs scaffold). #191 closes the M16-S4 triage-plan bookkeeping — either by landing a fix, by marking the items stale in the triage plan with a note, or by capturing them as scoped todos. Bundled because each is small and the verification gate (`cargo nextest --profile integration` for #180, `pytest tests/install-e2e/` for #174) overlaps.

**In scope.**

- #174 — `tests/install-e2e/test_update_recreate_classification.py` (or similar name) following the `test_update_multi_version.py` shape: install v, create + stop a session, run `sandbox update --dry-run --from <v'-tarball>` where the bumped tarball has `DAEMON_GUEST_PROTO_VERSION` advanced, assert the dry-run output emits per-session classification matching the runtime arm's compatibility check.
- #180 — In-process tests in `sandbox-cli/src/update/backup.rs` mirroring `dd58e36`'s shape: happy path, error arms, atomic-write rename invariants. Reuses the cfg_migrations test pattern as the template.
- #191 — Read MF12 + MF39 entries in `.tasks/handoffs/m16-s4-triage-plan.md`. For each: (a) close in this commit if still applicable and trivial, (b) mark stale in the triage plan with a note, or (c) capture as a scoped deferred todo with concrete commit-level scope.

**Explicitly deferred.**

- Final docs sweep → M16-S17 (this session ships code/test additions; docs reconciliation against the final shipping state is the next session's job).

**Exit criteria.**

- `cargo nextest run --workspace` default profile clean (1444 + new backup tests pass).
- New install-e2e test for recreate classification passes under `pytest tests/install-e2e/test_update_recreate_classification.py`.
- MF12 + MF39 disposition recorded in `.tasks/handoffs/m16-s4-triage-plan.md` (closed-by, stale, or new todo id with scope).
- Closed todos #174, #180, #191 marked resolved with this session's commit hashes.

---

## M16-S17 — Thorough documentation review and fixing

**Entry criteria.** M16-S16 complete — all code changes have landed, so documentation reflects the actual shipping state.

**Spec reference.** Every M16 spec: `../specs/2026-05-11-helper-identity-assertion-design/`, `../specs/2026-05-11-api-session-isolation-guest-compat-design/`, `../specs/2026-05-11-daemon-productionization-design/`, `../specs/2026-05-11-release-and-install-infrastructure-design/`, `../specs/2026-05-11-update-infrastructure-design/`. Final docs-vs-spec-vs-code reconciliation pass.

**Selected todos.** None — this is a polish session, not closing specific deferred todos. Its job is to catch drift the spec-delivery verification sessions (M15-S5, M16-S5) did not.

**Rationale.** Final sweep across every operator/developer-facing doc in `docs/`. The M16-S6..S16 arc landed substantial behavioural and structural changes (libexec sandbox-guest placement, Sigstore stack, drop of the daemon-side staging step, WAL/SHM rollback fix, peercred fix, test-naming changes); the closing sessions' docs touches were per-commit and may not have caught cross-cutting drift. This session re-reads every file in `docs/{reference,guides,operate,start,concepts}/`, cross-checks against the current code surface and the spec, and fixes every drift in-pass. Acts as the milestone-closing meta-verification — the docs equivalent of a spec-delivery verification session.

**In scope.**

- Walk every file under `docs/reference/`, `docs/guides/`, `docs/operate/`, `docs/start/`, `docs/concepts/`. For each, perform a structured pass:
  - Cross-check named CLI surfaces (`sandbox update`, `sandbox doctor`, `sandbox session`, `sandbox ps`, `sandbox rebuild-image`) against the current binary's `--help` output.
  - Cross-check spec citations (`Spec 1 § 8.4`, etc.) against the latest spec text — section headings move, paragraphs re-number.
  - Cross-check file paths (`/usr/local/libexec/sandboxd/sandbox-guest`, `/var/lib/sandbox/...`, audit log paths, install state file location) against current shipping reality.
  - Cross-check exit codes, env vars, and flag names against the latest CLI source.
  - Cross-check rendered examples (e.g. `sandbox update --check` output blocks) against the actual CLI output where the docs claim verbatim shape.
- Grep audit: zero milestone tags (`M16-Sxx`, `M15-Sxx`, etc.) anywhere under `docs/`.
- Markdown lint pass: broken cross-references resolve, no orphaned anchors, code blocks render, no truncated sections.
- For each drift found: fix in-pass in a focused commit (one logical doc per commit where practical) so review can land per-doc rather than as one monolithic diff.

**Explicitly deferred.**

- Spec text edits (the dropped spec-only todos #184, #186 are not re-opened here). This session reconciles docs to spec, not spec to code.
- New documentation surfaces — out of scope. If a missing surface surfaces (e.g. a feature shipped without docs), capture as a follow-on todo rather than authoring here.

**Exit criteria.**

- Every file in `docs/{reference,guides,operate,start,concepts}/` has been re-read and reconciled against current code + spec. Drifts found are fixed; drifts deferred are captured as scoped todos.
- `grep -rn -E "M[0-9]+-S[0-9]+" docs/` returns zero matches.
- All cross-references resolve to real anchors / pages.
- Operator-facing examples (CLI output blocks, file path listings) match shipping reality byte-for-byte where the docs claim verbatim shape.
- `markdownlint docs/` (or equivalent) clean.
- Code review approved.
