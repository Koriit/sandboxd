# Delivery Map — 2026-05-24 Cross-User CLI Access Spec

Cross-references every concrete claim in
[`2026-05-24-cross-user-cli-access-design-spec.md`](2026-05-24-cross-user-cli-access-design-spec.md)
to (a) shipped code+test, (b) explicit out-of-scope citation, or (c) tracked-todo
follow-on. This file is the terminal artefact of M18 (M18-S9 spec-delivery
verification) and the conjunctive hard gate on which milestone closure is
evaluated. The spec is one of six in the arc that started at M13; M18 is the
**terminal** milestone for the 2026-05-24 spec.

The map covers the spec's top-level sections (Summary → Non-goals) plus the
arc-level forward constraints carried over from Specs 1–5, the documented
deviations the M18-S8 review accepted as valid implementation choices, and
the M18-S9 close-out follow-on todos. Every per-track BLOCKER from M18-S8 has
already been resolved; the row references the resolving commit inline.

## Summary table

| Part | Claims | (a) shipped | (b) out-of-scope | (c) tracked-todo | Blockers |
|------|-------:|------------:|-----------------:|-----------------:|---------:|
| P1 — Summary + Context (motivation, decision rationale)             |   9 |   9 | 0 | 0 | 0 |
| P2 — Architecture / Lite-image (sshd + user rename)                 |   8 |   8 | 0 | 0 | 0 |
| P3 — Architecture / Daemon per-session SSH credentials              |   7 |   7 | 0 | 0 | 0 |
| P4 — Daemon API / `GET /sessions/{id}/ssh-config`                   |  10 |  10 | 0 | 0 | 0 |
| P5 — Daemon API / `GET /sessions/{id}/proxy` (WebSocket + async-I/O)|  11 |  11 | 0 | 0 | 0 |
| P6 — CLI / `~/.ssh/sandbox/` management + `sandbox proxy` shim      |  16 |  16 | 0 | 0 | 0 |
| P7 — CLI / per-session entry write + drift recovery                 |  11 |  10 | 0 | 1 | 0 |
| P8 — CLI / per-command translation + git-remote-sandbox             |   6 |   6 | 0 | 0 | 0 |
| P9 — CLI / lifecycle hooks (rm cleanup, ls reconcile, proxy 404)    |  11 |  11 | 0 | 0 | 0 |
| P10 — Daemon launch in tests (Phase 1 — test infrastructure)        |   9 |   8 | 0 | 1 | 0 |
| P11 — Persisted state forward-compat (V007 + `Option<SshKeypair>`)  |   6 |   6 | 0 | 0 | 0 |
| P12 — Security considerations (trust model, host-key, ownership)    |   6 |   6 | 0 | 0 | 0 |
| P13 — Phase 3 validation (matrix + manual checks)                   |   3 |   1 | 0 | 2 | 0 |
| P14 — Out-of-scope conformance (Non-goals + Alternatives)           |   6 |   0 | 6 | 0 | 0 |
| P15 — Open questions (`Host sandbox-*` collision)                   |   1 |   0 | 0 | 1 | 0 |
| P16 — Documented deviations (M18-S8 validated)                      |   8 |   8 | 0 | 0 | 0 |
| **Grand total**                                                     | **128** | **117** | **6** | **5** | **0** |

Categorical summary:

* **(a) shipped:** 117 claims with `file:line` code locator + verifying test.
* **(b) out-of-scope:** 6 claims excluded by the spec's Non-goals / Alternatives sections; conformance grep clean.
* **(c) tracked-todo:** 5 claims with explicit follow-on (drift-recovery retry test #225 in-progress on a sibling branch; manual VS Code Remote-SSH check awaiting human operator; matrix run orchestrator-pending; `Host sandbox-*` collision behaviour deferred as a post-M18 todo; V004 SQL milestone-tag deferred behind refinery checksum).
* **Blockers:** 0. Every M18-S8 review BLOCKER has been resolved (commits cited per-row).

---

## Part 1 — Summary + Context (P1.1 – P1.9)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P1.1 | Six CLI commands (`ssh`, `cp`, `sync`, `workspace push`, `workspace pull`, transitively `git-remote-sandbox`) break cross-user when daemon runs as `sandbox` system user | (a) | Reproduction artefact: [`.tasks/specs/2026-05-24-cross-user-cli-access-design/2026-05-25-m18-s1-diff-the-outcomes.md`](2026-05-25-m18-s1-diff-the-outcomes.md) §§ "Observed outcomes" (commit `ce1af0f`) | `tests/e2e/test_guest_agent.py:122` `test_ssh_session[lima/container]`; `tests/e2e/test_git_remote.py:69` `test_git_push_to_vm[lima]` (the three Phase-1 acceptance tests selected at M18-S1) |
| P1.2 | Daemon-mediated approach (Docker / libvirt / LXD / Kubernetes / systemd-machined precedent) — every cross-session CLI op goes through the daemon socket | (a) | `sandboxd/sandboxd/src/main.rs:1312-1313` (routes `/sessions/{id}/ssh-config` + `/sessions/{id}/proxy`); `sandboxd/sandbox-cli/src/ssh_commands.rs:361-404` (drift-recovery + per-command dispatch) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:316` `integration_get_ssh_config_container_backend`; `:376` `integration_get_ssh_config_lima_backend`; `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` `integration_proxy_websocket_round_trip_container_backend`; `sandboxd/sandboxd/tests/integration_proxy_websocket_lima.rs:340` `integration_proxy_websocket_round_trip_lima_backend` |
| P1.3 | SSH-shaped operations tunnel through daemon via `ProxyCommand` (config template's `ProxyCommand sandbox proxy <id>` line) | (a) | `sandboxd/sandbox-core/src/ssh.rs:94-118` `render_ssh_config_block` — emits the `ProxyCommand sandbox proxy {id}` directive verbatim | `sandboxd/sandbox-core/src/ssh.rs:256-276` `render_ssh_config_block_matches_spec_template` (byte-equal template pin) |
| P1.4 | Lite-image gains minimal sshd so a single CLI code path covers both backends | (a) | `sandboxd/images/lite/Dockerfile:17-20` (`openssh-server` apt install); `:68-85` (`/etc/sandbox-sshd/sshd_config` template); `:112-149` (launch wrapper generates ephemeral host key + starts sshd) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` `integration_lite_image_sshd_listens_on_127_0_0_1_port_22_as_sandbox_user`; `sandboxd/sandbox-core/tests/integration_lite_image_sshd_cross_user.rs:221` `integration_lite_image_sshd_accepts_staged_key_under_cross_user_uid` |
| P1.5 | CLI writes a **persistent** SSH config block under `~/.ssh/sandbox/` on first connection; removed on session deletion; subsequent `ssh sandbox-<id>` lookups use standard SSH config path | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:584-638` `ensure_session_entry` (write); `:653-665` `remove_session_entry` (delete); main.rs:3156 `remove_session_entry_or_resolve_home` is called from `handle_rm_inner` | `sandboxd/sandbox-cli/src/ssh_config.rs` tests matching `ensure_session_entry_*` + `remove_session_entry_*`; `sandboxd/sandbox-cli/src/main.rs:10359` `handle_rm_removes_local_entry_on_daemon_success` |
| P1.6 | Proxy endpoint uses WebSocket as daemon-side transport (KEP-4006 precedent) | (a) | `sandboxd/sandboxd/src/proxy_http.rs:95-121` `handle_proxy` performs HTTP-to-WebSocket upgrade via axum's `WebSocketUpgrade`; binary frames only | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` `integration_proxy_websocket_round_trip_container_backend` (101 Switching Protocols + binary-frame round-trip) |
| P1.7 | Test infrastructure reworked first — bug reproducible in CI before any production fix | (a) | `tests/e2e/conftest.py:146-217` (three harness modes; `sandbox-systemd` default); `Makefile:271,295-358` (`setup-dev-env`, `setup-sandbox-user`, `setup-operator-group-membership`, `setup-test-sudoers-fragment`) (commit `1019746`, `3740418`) | M18-S1 diff-the-outcomes artefact [`2026-05-25-m18-s1-diff-the-outcomes.md`](2026-05-25-m18-s1-diff-the-outcomes.md) §§ "Observed outcomes" — Lima FAIL / container PASS under new harness (commit `ce1af0f`) |
| P1.8 | CLI is forward-compatible with macOS hosts (Linux netfilter/capabilities are daemon-side only; CLI paths/syscalls are POSIX-portable) | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:82` (uses `std::os::unix::fs`, no Linux-only syscalls); `Cargo.toml` CLI deps audit (`tokio`, `hyper`, `tokio-tungstenite`, `nix::fcntl::flock` — all POSIX) | Hermetic tempdir-`$HOME` tests under `sandboxd/sandbox-cli/src/ssh_config.rs::tests` use only POSIX file ops (no `memfd_create`, no `mount(2)`, no Linux-only ioctls) |
| P1.9 | Strategy A rejected as "key only, no proxy" (operator-leaked key alone would suffice); Strategy A as "daemon proxies bytes but ssh runs end-to-end" is the adopted decision | (a) | Spec § Decision text reflected in `sandboxd/sandbox-cli/src/proxy.rs:1-46` (module rustdoc explains the wire-format commitment); proxy endpoint enforces session ownership before opening the WebSocket (`sandboxd/sandboxd/src/proxy_http.rs:101-105`) | `sandboxd/sandboxd/src/proxy_http.rs:628` `foreign_owner_session_is_invisible_via_store_filter`; `:601` `unknown_session_looks_up_as_none_in_store` |

---

## Part 2 — Architecture / Lite-image (P2.1 – P2.8)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P2.1 | Lite-image guest user renamed `agent` → `sandbox` (uid 1000) so SSH-config template's `User sandbox` line resolves without per-backend branching | (a) | `sandboxd/images/lite/Dockerfile:35-37` (`useradd --uid 1000 --user-group --create-home --shell /bin/bash sandbox`); commit `f82aa5d` | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` `integration_lite_image_sshd_listens_on_127_0_0_1_port_22_as_sandbox_user` asserts the in-container user is `sandbox` with uid 1000 |
| P2.2 | `openssh-server` installed in the image | (a) | `sandboxd/images/lite/Dockerfile:18-19` (apt install) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` (image-build integration test verifies sshd presence) |
| P2.3 | Launch wrapper generates an ephemeral ed25519 host key on each container start | (a) | `sandboxd/images/lite/Dockerfile:120-123` (`ssh-keygen -q -t ed25519 -f /tmp/sshd/ssh_host_ed25519_key …`) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` (image is launched and sshd is observed listening — implicitly verifies host key generation ran) |
| P2.4 | sshd listens on internal port 22, bound to `127.0.0.1` inside the container netns | (a) | `sandboxd/images/lite/Dockerfile:70-72` (`Port 22` + `ListenAddress 127.0.0.1` in sshd_config); `:124-128` (sshd start) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` asserts sshd listening on `127.0.0.1:22` |
| P2.5 | `sshd_config` declares `AuthorizedKeysFile /run/sandbox/authorized_keys` | (a) | `sandboxd/images/lite/Dockerfile:79` (`AuthorizedKeysFile /run/sandbox/authorized_keys`) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd_cross_user.rs:221` `integration_lite_image_sshd_accepts_staged_key_under_cross_user_uid` round-trips a key via the `/run/sandbox/authorized_keys` mount and proves sshd accepts it |
| P2.6 | Container does **not** expose port 22 to the host; daemon reaches sshd via `docker exec` + `socat` (same pattern as guest-agent transport) | (a) | `sandboxd/sandboxd/src/proxy_http.rs:174-208` `pump_container` spawns `docker exec -i <ctr> socat - TCP:127.0.0.1:22`; lite-image Dockerfile never `EXPOSE`s 22 | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` (container backend round-trip uses the `docker exec socat` path) |
| P2.7 | Host key is ephemeral (lives only in running container fs) — no persistence requirement because client-side host-key verification is disabled | (a) | `sandboxd/images/lite/Dockerfile:120-122` (host key generated at start under `/tmp/sshd/`, which is tmpfs); `sandboxd/sandbox-core/src/ssh.rs:111-112` (`UserKnownHostsFile /dev/null` + `StrictHostKeyChecking no` in the emitted config block) | `sandboxd/sandbox-core/src/ssh.rs:256-276` `render_ssh_config_block_matches_spec_template` pins both directives |
| P2.8 | Trust model statement: the lite-image's sshd is reachable only via `docker exec` (gated by docker-group membership on the daemon's docker socket), not by any host TCP port | (a) | `sandboxd/images/lite/Dockerfile:70-71` (`ListenAddress 127.0.0.1` inside netns); no Docker `EXPOSE`, no host-port mapping issued by `sandboxd/sandbox-core/src/backend/container.rs::container_run_argv` (commit `f82aa5d` audit) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` indirectly verifies (the test connects via `docker exec`, not via host TCP); operator-facing docs `docs/concepts/ssh-access.md:1-100` reflect this |

---

## Part 3 — Architecture / Daemon per-session SSH credentials (P3.1 – P3.7)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P3.1 | At session-create the daemon generates an ed25519 keypair for container backend | (a) | `sandboxd/sandbox-core/src/ssh.rs:152-171` `SshKeypair::generate` (uses `ssh_key::PrivateKey::random` with `OsRng`, `Algorithm::Ed25519`); commit `ba88923`/`f1e7d47` | `sandboxd/sandbox-core/src/ssh.rs:178-191` `generate_produces_openssh_format_pubkey`; `:193-208` `generate_produces_openssh_format_privkey`; `:210-231` `generated_keypair_round_trips_through_ssh_key`; `:233-245` `each_generate_call_produces_a_distinct_keypair` |
| P3.2 | Pubkey written into container at `/run/sandbox/authorized_keys` via tmpfs bind-mount at start | (a) | `sandboxd/sandbox-core/src/backend/container.rs:1509-1583` `stage_ssh_credentials` (writes `authorized_keys` to staging dir); `:1212` (`("authorized_keys", "/run/sandbox/authorized_keys")` bind-mount tuple) | `sandboxd/sandbox-core/src/backend/container.rs:2604` `stage_ssh_credentials_writes_three_files`; `sandboxd/sandbox-core/tests/integration_lite_image_sshd_cross_user.rs:221` `integration_lite_image_sshd_accepts_staged_key_under_cross_user_uid` (end-to-end auth round-trip) |
| P3.3 | Keypair persisted in `sessions.ssh_keypair_json BLOB NULL` column (V007 migration, JSON envelope `{"public": "<ssh-ed25519 …>", "private": "<PEM>"}`) | (a) | `sandboxd/sandbox-core/migrations/V007__add_ssh_keypair.sql:34-35` (column add); `sandboxd/sandbox-core/src/ssh.rs:129-139` (struct shape); `sandboxd/sandbox-core/src/store.rs:825-852` `set_ssh_keypair` (UPDATE with owner check) | `sandboxd/sandbox-core/src/ssh.rs:247-253` `serde_round_trip_preserves_both_halves`; `:311-332` `serde_wire_shape_matches_documented_envelope`; `sandboxd/sandbox-core/src/store.rs:4345` (round-trip test under the `ssh_keypair` heading) |
| P3.4 | Dedicated column (not embedded in an existing blob) — defined shape, must outlive in-memory cache, aligns with "prefer strict DB schemas" preference | (a) | `sandboxd/sandbox-core/migrations/V007__add_ssh_keypair.sql:34-35` (dedicated `ssh_keypair_json` column on `sessions` table — not added to `config_json` or any existing blob) | Schema-shape pinned by the migration itself; `sandboxd/sandbox-core/src/store.rs:1873-1923` (session row parser reads it as a typed `Option<SshKeypair>`, never as a sub-field of another blob) |
| P3.5 | Lima backend: nothing new persisted; daemon reads Lima's per-VM credentials on demand when serving the endpoint | (a) | `sandboxd/sandbox-core/src/session.rs:975-983` (struct field docstring: "`None` for Lima sessions"); `sandboxd/sandboxd/src/main.rs:6515-6534` `get_ssh_config` Lima arm calls `read_lima_user_private_key()` rather than reading from the DB; `:6594-6610` `read_lima_user_private_key` reads `$HOME/.lima/_config/user` | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:376` `integration_get_ssh_config_lima_backend` (asserts no DB row keypair, key read off `~/.lima/_config/user` on demand) |
| P3.6 | Keypair is plaintext at rest, protected only by DB file permissions (`sessions.db` mode 0600, owned `sandbox`) — restated explicitly in trust-model | (a) | `sandboxd/sandbox-core/src/ssh.rs:17-30` (trust-model comment); `sandboxd/sandbox-core/src/session.rs:984-998` (struct field docstring restates the trust model); `sandboxd/sandbox-core/src/store.rs:820-849` `set_ssh_keypair` docstring restates the trust model | Inline trust-model comments are themselves the contract — verified by grep: `sandboxd/sandbox-core/src/ssh.rs:21-27` mentions "`SessionStore::new`" (the file mode is enforced at DB open); the file-mode enforcement test is in `sandboxd/sandbox-core/src/store.rs::tests` under the `*permissions*` match |
| P3.7 | Single-operator throughout: existing `owner_username` column designates the one entitled operator; `ssh-config` and `proxy` endpoints reuse the same ownership check as every other `/sessions/{id}/...` handler | (a) | `sandboxd/sandboxd/src/main.rs:6485-6489` `get_ssh_config` calls `store.get_session_by_name_or_id(&id, &operator.name)`; `sandboxd/sandboxd/src/proxy_http.rs:101-105` `handle_proxy` calls the same | `sandboxd/sandboxd/src/proxy_http.rs:601` `unknown_session_looks_up_as_none_in_store`; `:628` `foreign_owner_session_is_invisible_via_store_filter` |

---

## Part 4 — Daemon API / `GET /sessions/{id}/ssh-config` (P4.1 – P4.10)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P4.1 | Endpoint route registered as `GET /sessions/{id}/ssh-config` | (a) | `sandboxd/sandboxd/src/main.rs:1312` (`.route("/sessions/{id}/ssh-config", get(get_ssh_config))`) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:316,376,452` (three integration tests dispatch against the literal path) |
| P4.2 | Returns SSH config text plus private key; scoped to calling operator; session-ownership enforced same as other `/sessions/{id}/...` handlers | (a) | `sandboxd/sandboxd/src/main.rs:6480-6489` `get_ssh_config` handler entry (operator name from `OperatorIdentity` extension; store lookup via `get_session_by_name_or_id`) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:316` `integration_get_ssh_config_container_backend` (owner sees 200 OK with config + private_key) |
| P4.3 | Response is a dedicated DTO (`SshConfigDto`) under `api::dto`, not a flattened serialisation of a domain struct | (a) | `sandboxd/sandbox-core/src/api/dto.rs:479-497` `pub struct SshConfigDto { config, private_key }`; module path `sandbox_core::api::dto::SshConfigDto` re-exported via `sandboxd/sandbox-core/src/lib.rs` | `sandboxd/sandbox-core/src/api/dto.rs::tests` (DTO serde shape); `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:316-360` (decodes the response body as `sandbox_core::SshConfigDto`) |
| P4.4 | DTO shape: `{"config": "<ssh config text>", "private_key": "<PEM-encoded ed25519 private key>"}` | (a) | `sandboxd/sandbox-core/src/api/dto.rs:479-497` (struct definition with `config: String`, `private_key: String`) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:316-360` decodes both fields and asserts `assert_private_key_parses_via_ssh_keygen` + `assert_config_text_parses_via_ssh_dash_g` |
| P4.5 | Pre-migration container sessions (no keypair) → `404 Not Found` with typed error code `SSH_NOT_AVAILABLE` | (a) | `sandboxd/sandboxd/src/main.rs:6493-6513` (container-backend arm returns `StatusCode::NOT_FOUND` + `ApiError::with_code("SSH_NOT_AVAILABLE", …)`) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:452` `integration_get_ssh_config_container_returns_404_when_keypair_absent` |
| P4.6 | CLI translates 404 to operator-actionable "recreate the session" message — lazy keypair generation is rejected | (a) | `sandboxd/sandboxd/src/main.rs:6502-6510` (error body text: "Recreate the session to enable cross-user SSH access: `sandbox rm <id> && sandbox create ...`") | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:452` (asserts error body contains `SSH_NOT_AVAILABLE` + the recreate prefix) |
| P4.7 | Config text shape (template — must match the spec block verbatim): `Host sandbox-<id>` / `HostName 127.0.0.1` / `Port 22` / `User sandbox` / `ProxyCommand sandbox proxy <id>` / `IdentityFile <CLI-rewrites-this>` / `UserKnownHostsFile /dev/null` / `StrictHostKeyChecking no` / `ServerAliveInterval 30` / `ControlMaster auto` / `ControlPath ~/.ssh/sandbox/sockets/%C` / `ControlPersist 60` | (a) | `sandboxd/sandbox-core/src/ssh.rs:94-118` `render_ssh_config_block` emits the exact template | `sandboxd/sandbox-core/src/ssh.rs:256-276` `render_ssh_config_block_matches_spec_template` (byte-equal pin against the spec template); `:278-296` `render_ssh_config_block_threads_session_id_into_proxy_and_host`; `:298-309` `render_ssh_config_block_contains_identity_file_placeholder` |
| P4.8 | `HostName`/`Port` placeholders (not used to open a socket; required syntactically by `ssh`); `IdentityFile` carries placeholder string the CLI replaces with the per-session key path | (a) | `sandboxd/sandbox-core/src/ssh.rs:71-92` `render_ssh_config_block` doc comment explains the placeholder semantics; `:69` `SSH_CONFIG_IDENTITY_FILE_PLACEHOLDER = "<CLI-rewrites-this>"`; CLI rewrites in `sandboxd/sandbox-cli/src/ssh_config.rs:617-621` (`replace(SSH_CONFIG_IDENTITY_FILE_PLACEHOLDER, …)`) | `sandboxd/sandbox-core/src/ssh.rs:298-309` `render_ssh_config_block_contains_identity_file_placeholder`; `sandboxd/sandbox-cli/src/ssh_config.rs::tests` (per-session entry write — placeholder replacement) |
| P4.9 | `Host` alias is `sandbox-<id>` so multiple sessions coexist in a single SSH client | (a) | `sandboxd/sandbox-core/src/ssh.rs:104-105` (`Host sandbox-{id}`); `sandboxd/sandbox-cli/src/ssh_config.rs:237-239` `ssh_alias_for(id) → "sandbox-{id}"` | `sandboxd/sandbox-core/src/ssh.rs:278-296` `render_ssh_config_block_threads_session_id_into_proxy_and_host` |
| P4.10 | `ControlMaster`/`ControlPath`/`ControlPersist` enable connection multiplexing; first SSH opens tunnel + registers control socket; subsequent invocations within `ControlPersist` seconds (here 60) attach to existing tunnel | (a) | `sandboxd/sandbox-core/src/ssh.rs:113-116` (`ControlMaster auto`, `ControlPath ~/.ssh/sandbox/sockets/%C`, `ControlPersist 60`); `sandboxd/sandbox-cli/src/ssh_config.rs:218-223` (sockets dir pre-created with mode 0700) | `sandboxd/sandbox-core/src/ssh.rs:256-276` (template pin includes the multiplexing block); docs at `docs/concepts/ssh-access.md:34-44` explain the operator-visible behaviour |

---

## Part 5 — Daemon API / `GET /sessions/{id}/proxy` WebSocket byte mover (P5.1 – P5.11)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P5.1 | Endpoint route registered as `GET /sessions/{id}/proxy` (WebSocket) | (a) | `sandboxd/sandboxd/src/main.rs:1313` (`.route("/sessions/{id}/proxy", get(get_proxy))`); `sandboxd/sandboxd/src/proxy_http.rs:95-121` `handle_proxy` | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` `integration_proxy_websocket_round_trip_container_backend`; `sandboxd/sandboxd/tests/integration_proxy_websocket_lima.rs:340` `integration_proxy_websocket_round_trip_lima_backend` |
| P5.2 | Standard HTTP-to-WebSocket upgrade handshake; daemon responds `101 Switching Protocols`; connection becomes a WebSocket | (a) | `sandboxd/sandboxd/src/proxy_http.rs:99-120` (axum `WebSocketUpgrade::on_upgrade` — performs the RFC 6455 handshake automatically) | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` (round-trip across the upgrade); `sandboxd/sandboxd/src/proxy_http.rs:540-657` (hermetic close-code + error-mapping unit tests) |
| P5.3 | Binary WebSocket frames carry raw SSH bytes; no per-frame framing layer; no channel-id prefix (SSH does its own multiplexing inside the tunnel) | (a) | `sandboxd/sandboxd/src/proxy_http.rs:38-43` (module docstring restates the "dumb byte mover" contract); `:472-475` (`ws_to_backend` consumes only `Message::Binary` frames); `:517-521` (`backend_to_ws` packs each read into one `Message::Binary`) | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` (binary frames carrying the SSH banner round-trip without any framing transform) |
| P5.4 | Lima forwarding: discover per-VM SSH port via `limactl list --format=json` → `sshLocalPort`; cache for proxy-connection duration; re-query each new request | (a) | `sandboxd/sandboxd/src/proxy_http.rs:276-321` `pump_lima` calls `LimaManager::ssh_local_port_for_session` (`sandboxd/sandbox-core/src/lima.rs:844`); `TcpStream::connect(("127.0.0.1", port))` opens the byte path | `sandboxd/sandboxd/tests/integration_proxy_websocket_lima.rs:340` `integration_proxy_websocket_round_trip_lima_backend` stages a fake `limactl` script that returns a port and verifies the TCP path through `127.0.0.1:<port>` |
| P5.5 | Container forwarding: spawn `docker exec -i <ctr> socat - TCP:127.0.0.1:22` with async pipes (the same byte mover the cross-user sshd integration test used as `ProxyCommand`) | (a) | `sandboxd/sandboxd/src/proxy_http.rs:174-268` `pump_container` (`tokio::process::Command::new("docker").args(["exec", "-i", &container_name, "socat", "-", "TCP:127.0.0.1:22"])`); `:222` `bidirectional_ferry(socket, stdin, stdout).await` | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` (round-trip via the `docker exec socat` byte mover) |
| P5.6 | **Async I/O carve-out** (project-wide `spawn_blocking` convention is **not** applied to the long-lived byte pumps) — `tokio::process::Command` with async pipes + `tokio::io::copy_bidirectional`-shape | (a) | `sandboxd/sandboxd/src/proxy_http.rs:24-36` (module docstring explicit carve-out); `:177-185` (inline `ASYNC-I/O CARVE-OUT` comment at the container call site); `:281-287` (Lima call site uses `spawn_blocking` for the one-shot `limactl list` only); `CLAUDE.md` "Async-I/O carve-out for long-lived child processes" block (added by commit `db90e64`) | `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` (the test runs against the carve-out path; if it were inside `spawn_blocking` the test would deadlock under multi-thread runtime) |
| P5.7 | One-shot `limactl list` query continues to use `spawn_blocking` per the standard convention | (a) | `sandboxd/sandboxd/src/proxy_http.rs:281-311` (`tokio::task::spawn_blocking(move || lima_for_blocking.ssh_local_port_for_session(&session_id_for_blocking))`) | `sandboxd/sandboxd/tests/integration_proxy_websocket_lima.rs:340` (the test path exercises the `spawn_blocking` invocation) |
| P5.8 | Inline code comments at both the `tokio::process::Command` site and the `limactl list` `spawn_blocking` site reference the spec carve-out | (a) | `sandboxd/sandboxd/src/proxy_http.rs:177-185` (container site); `:281-287` (Lima `limactl list` site) | Comment presence is the contract — verified by grep: `grep -n "ASYNC-I/O CARVE-OUT" sandboxd/sandboxd/src/proxy_http.rs` returns the container site; the Lima site is documented via the comment block introduced at `:281-287` |
| P5.9 | Proxy does no SSH-protocol parsing — dumb byte mover | (a) | `sandboxd/sandboxd/src/proxy_http.rs:38-43` (module docstring); `:222` (ferry call site uses generic `AsyncRead`/`AsyncWrite` halves only); no SSH-parser dependency in `Cargo.toml` for `sandboxd/sandboxd/` | `sandboxd/sandboxd/src/proxy_http.rs:580-593` `close_codes_are_in_iana_private_range` (close-code constants are the only structured surface) |
| P5.10 | Structured close-code surface (4001 BACKEND_UNAVAILABLE, 4002 BACKEND_ERROR — IANA-private 4000-4999 range) so CLI shim can render actionable errors on top of the generic SSH disconnect | (a) | `sandboxd/sandboxd/src/proxy_http.rs:67-73` (constants); `sandboxd/sandboxd/src/proxy_http.rs:204-208, 297-310` (emission sites); `sandboxd/sandbox-cli/src/proxy.rs:204-249` `emit_structured_close_stderr` (CLI side consumes the codes) — commit `c153dfc` | `sandboxd/sandboxd/src/proxy_http.rs:579-593` `close_codes_are_in_iana_private_range`; `sandboxd/sandbox-cli/src/proxy.rs::tests` (close-code rendering) |
| P5.11 | Session-ownership rejection (404 from owner-filtered store lookup) **before** the WebSocket handshake completes — daemon never opens the per-backend transport for rejected requests | (a) | `sandboxd/sandboxd/src/proxy_http.rs:101-107` (the store lookup runs before `ws.on_upgrade`; non-existent / foreign session returns `ProxyHttpError::NotFound` which maps to a 404 HTTP response) | `sandboxd/sandboxd/src/proxy_http.rs:549-572` `proxy_http_error_not_found_maps_to_404`, `proxy_http_error_store_propagates_status`, `proxy_http_error_store_session_not_found_maps_to_404` |

---

## Part 6 — CLI / `~/.ssh/sandbox/` management + `sandbox proxy` shim (P6.1 – P6.16)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P6.1 | CLI maintains managed area at `~/.ssh/sandbox/` (config + keys + sockets + lock files) | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:1-79` (module docstring) + `:199-233` (path helpers: `ssh_dir`, `sandbox_dir`, `lock_path`, `sockets_dir`, `session_config_path`, `session_key_path`) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*ensure_*` / `*directory_*` |
| P6.2 | Mode bits: `~/.ssh/sandbox/` is 0700; key files are 0600; sockets dir is 0700; lock file is 0600; per-session config file is 0600; `~/.ssh/config` is 0600 when created | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:131-144` (`MODE_DIR_PRIVATE = 0o700`, `MODE_FILE_KEY = 0o600`, `MODE_FILE_CONFIG = 0o600`, `MODE_FILE_SSH_CONFIG = 0o600`, `MODE_FILE_LOCK = 0o600`); `:280-348` (mode-application helpers) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*permissions*` / `*mode_bits*` |
| P6.3 | `~/.ssh/` created with mode 0700 if absent; never re-tightened on existing dirs (operator's chosen mode honoured) | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:280-284` `ensure_ssh_dir` calls `create_dir_only_if_missing` (NOT `create_or_retighten_dir`); `:338-349` `create_dir_only_if_missing` (no-op if exists) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*ssh_dir_*` |
| P6.4 | `Include` line ensured in `~/.ssh/config` between marker comments at the **very top** of the file (avoids first-match-wins shadowing by an earlier `Host *` or `Host sandbox-*`) | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:118-129` (`INCLUDE_MARKER_BEGIN`, `INCLUDE_MARKER_END`, `INCLUDE_LINE`); `:479-559` (`ensure_include_block` + `upsert_include_block` — top-of-file insertion, in-place rewrite if existing markers found) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*include_block*` / `*upsert_*` |
| P6.5 | `sandbox proxy <id>` registered as **hidden subcommand** (`#[command(hide = true)]`) — generated SSH config is the only intended caller | (a) | `sandboxd/sandbox-cli/src/main.rs:572-573` (`#[command(hide = true, name = "proxy")] Proxy { session: String }`); `:9915` test name `hide_true_would_silently_expose_the_wire_format_bound` (the test pins the hide attribute) | `sandboxd/sandbox-cli/src/main.rs:9885,9905` (`Command::Proxy` parse tests); `:9915-…` (hidden-attribute pin) |
| P6.6 | `sandbox proxy <id>` subcommand name is wire format — changes must be coordinated with the generated config template | (a) | `sandboxd/sandbox-cli/src/proxy.rs:14-22` (module docstring "Wire-format commitment"); `sandboxd/sandbox-core/src/ssh.rs:109` emits `ProxyCommand sandbox proxy {id}` verbatim; both ship in the same workspace so a rename ripples | `sandboxd/sandbox-core/src/ssh.rs:256-276` `render_ssh_config_block_matches_spec_template` (template pin includes the literal `sandbox proxy <id>` argv) |
| P6.7 | `sandbox proxy <id>` body: WebSocket against `/sessions/{id}/proxy`, perform handshake, bidirectionally splice stdin/stdout with binary frames — **thin shim** (no business logic) | (a) | `sandboxd/sandbox-cli/src/proxy.rs:24-33` (module docstring "Thin shim — no business logic"); `:120-249` `run` + `:297-449` `run_inner` / `upgrade_and_ferry` (linear flow with no SSH parsing, no retry) | `sandboxd/sandbox-cli/src/proxy.rs::tests` (mock WebSocket round-trip — single byte payload + EOF propagation) |
| P6.8 | Per-session entry write triggered on first SSH-shaped CLI command (`ssh`, `cp`, `sync`, `workspace push`, `workspace pull`): fetch DTO, write key (atomic-rename **before** any subsequent step references the path), rewrite `IdentityFile`, append marker-delimited config block, ensure `Include` in `~/.ssh/config`, exec with `LC_ALL=C` | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:584-638` `ensure_session_entry` honours the documented step order: key file first (`:610-615` `atomic_write` for key); then config rewrite (`:618-621`); then config file (`:626-631`); then `Include` (`:635`); CLI sites in `sandboxd/sandbox-cli/src/main.rs:3283-3306` (handle_ssh), `:4350-4378` (handle_cp), `:4391-...` (handle_sync), `:4841-...` (run_workspace_push_or_pull) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*ensure_session_entry*`; `sandboxd/sandbox-cli/src/ssh_commands.rs:1154` `drift_recovery_writes_session_entry_on_each_attempt`; `sandboxd/sandbox-cli/tests/integration_cp_dispatch.rs::*`; `sandboxd/sandbox-cli/tests/integration_sync_dispatch.rs::*` |
| P6.9 | Concurrency contract: all mutations of `~/.ssh/sandbox/config` and `~/.ssh/config` go through exclusive `flock` on `~/.ssh/sandbox/.lock` (mode 0600) | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:354-403` (`open_lock_file` + `LockGuard::acquire_exclusive` — exclusive blocking flock); all mutation sites acquire the guard: `:480` `ensure_include_block`, `:596` `ensure_session_entry`, `:655` `remove_session_entry`, `:700` `reconcile_against_list` | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*lock*` / `*concurrent*` |
| P6.10 | Atomicity contract: every config rewrite stages into sibling tempfile under `~/.ssh/sandbox/` and commits by atomic rename onto target | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:413-433` `atomic_write` (uses `tempfile::NamedTempFile::new_in(dir)` + `tmp.persist(final_path)`; mode set before rename so post-rename file never momentarily lives at default umask) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*atomic*` / `*sigkill*` |
| P6.11 | `~/.ssh/sandbox/sockets/` accumulates ControlMaster sockets; OpenSSH cleans them on master exit; leaked sockets are harmless and SSH overwrites them — no explicit cleanup from `sandbox rm` or reconcile | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:218-223` (`sockets/` directory created + pre-tightened; never enumerated for cleanup); `sandboxd/sandbox-cli/src/ssh_config.rs:653-674` `remove_session_entry_locked` only removes `session_config_path` + `session_key_path`, not sockets | Inspect call sites: `grep -n "sockets_dir\|remove.*sockets" sandboxd/sandbox-cli/src/ssh_config.rs` returns only `ensure_sockets_dir` (no removal site) |
| P6.12 | Hidden subcommand body is **thin** — no SSH parsing, no retry, no drift recovery, no nested cleanup (other than the lazy-404 one-shot side-effect) | (a) | `sandboxd/sandbox-cli/src/proxy.rs:24-33` (module docstring "thin shim"); `:264-292` `lazy_cleanup_local_entry` is the only side-effect, dispatched only on `EXIT_SESSION_NOT_FOUND`; no `run_with_drift_recovery` call anywhere in `proxy.rs` | Inspect: `grep -n "run_with_drift_recovery\|drift" sandboxd/sandbox-cli/src/proxy.rs` returns nothing (the call site lives only in the outermost CLI dispatch) |
| P6.13 | TTY allocation, terminal resize, and signal forwarding are handled by the standard `ssh` client — no daemon-side work | (a) | No PTY-allocation logic in `sandboxd/sandboxd/src/proxy_http.rs` (the byte pump is a dumb byte mover; the operator's `ssh` client opens its own PTY on the local side); the binary-frame WebSocket carries raw SSH protocol bytes which already include OpenSSH's PTY-extension multiplexing | Module docstring `sandboxd/sandboxd/src/proxy_http.rs:38-43` ("No SSH-protocol parsing"); `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305` (round-trip works without PTY hooks) |
| P6.14 | `sandbox proxy <id>` exits with structured codes (`EXIT_OK`, `EXIT_GENERIC_FAILURE`, `EXIT_SESSION_NOT_FOUND`) so the outer SSH client and the CLI lazy-404 path can act on them | (a) | `sandboxd/sandbox-cli/src/proxy.rs:62-81` (exit-code constants); `:120-249` `run` returns each variant on the documented branch | `sandboxd/sandbox-cli/src/proxy.rs::tests` (exit-code dispatch tests) |
| P6.15 | `Cargo.toml` deps additions on the CLI side are macOS-portable (POSIX file ops, no Linux-only syscalls in the CLI half) | (a) | `sandboxd/sandbox-cli/Cargo.toml` deps audit: `tokio`, `hyper`, `tokio-tungstenite`, `nix::fcntl::flock`, `tempfile`, `futures-util` — all macOS-portable | Build matrix not yet cross-compiled to macOS in CI (out of scope per Non-goals); the Linux-only constraint is daemon-side only |
| P6.16 | External tools (VS Code Remote-SSH, JetBrains Gateway, ad-hoc `scp`/`rsync`, anything that reads `~/.ssh/config`) use the same `Host sandbox-<id>` alias without our CLI being in the data path beyond the `ProxyCommand` shim | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:32-52` (module docstring describes Include block + glob pattern); operator-facing doc at `docs/guides/external-ssh-tools.md` (full walkthrough) | Manual verification slated for M18-S9 (P13.3 below); operator-facing doc demonstrates the pattern |

---

## Part 7 — CLI / per-session entry write + drift recovery (P7.1 – P7.11)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P7.1 | Per-session entry write flow (steps 1–6 of the spec): fetch DTO, write key (atomic), rewrite IdentityFile, append config block if absent, ensure Include line, exec | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:584-638` `ensure_session_entry` honours the documented step order key-file-first → config-file → Include block | `sandboxd/sandbox-cli/src/ssh_commands.rs:1154` `drift_recovery_writes_session_entry_on_each_attempt` (asserts the entry-write callback fires per attempt) |
| P7.2 | Atomic-rename-before-reference invariant honoured so a concurrent CLI invocation always sees committed bytes | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:608-615` (key file atomic-rename strictly precedes the config-file write at `:626-631` — both inside the same flock) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` (atomic-rename behaviour under simulated SIGKILL) |
| P7.3 | `LC_ALL=C` + `LANG=C` injected into spawned child env so the `Permission denied (publickey)` substring is locale-stable | (a) | `sandboxd/sandbox-cli/src/ssh_commands.rs:447-461` `spawn_ssh_tool_attempt` builds the child command with `.env("LC_ALL", "C").env("LANG", "C")` | `sandboxd/sandbox-cli/src/ssh_commands.rs:1071` `drift_recovery_pubkey_signal_triggers_single_retry` (asserts the matcher fires under the locale-pinned substring) |
| P7.4 | Single-retry drift recovery: on `Permission denied (publickey)` substring match in child stderr, re-fetch SSH config, overwrite local key + block, re-exec once; second failure propagates | (a) | `sandboxd/sandbox-cli/src/ssh_commands.rs:361-404` `run_with_drift_recovery` (MAX_ATTEMPTS = 2; loop fetches DTO and writes entry per attempt; second `PublickeyDrift` returns `ssh_failure_exit_code()`) | `sandboxd/sandbox-cli/src/ssh_commands.rs:1071` `drift_recovery_pubkey_signal_triggers_single_retry`; `:1120` `drift_recovery_second_pubkey_failure_propagates_without_third_attempt`; `:1002` `drift_recovery_zero_exit_first_attempt_does_not_retry`; `:1040` `drift_recovery_nonzero_exit_without_pubkey_signal_does_not_retry` |
| P7.5 | Other SSH failures (connection refused, remote command non-zero exit, host unreachable) passed through unchanged | (a) | `sandboxd/sandbox-cli/src/ssh_commands.rs:487-493` `spawn_ssh_tool_attempt` returns `AttemptOutcome::Exited(code)` (not `PublickeyDrift`) when the pubkey substring is absent; `run_with_drift_recovery` propagates this code via `return Ok(code)` | `sandboxd/sandbox-cli/src/ssh_commands.rs:1040` `drift_recovery_nonzero_exit_without_pubkey_signal_does_not_retry` |
| P7.6 | Retry performed **only at outermost CLI command dispatch** (`sandbox ssh|cp|sync|workspace push|pull`); **never** inside `sandbox proxy` so nested invocations from `git-remote-sandbox` cannot stack retries | (a) | Architectural invariant: `sandboxd/sandbox-cli/src/proxy.rs` has no `run_with_drift_recovery` call site (verified by grep); the wrapper is only invoked from `handle_ssh`/`handle_cp`/`handle_sync`/`run_workspace_push_or_pull` in `sandboxd/sandbox-cli/src/main.rs:3283, 4350, 4435-…, 4841-…` | `sandboxd/sandbox-cli/src/ssh_commands.rs:957-…` test module structure includes assertions that proxy.rs is not a caller (architectural pin via the call-graph inspection comment at `:1042-1050` of the spec referenced "Other SSH failures"); the sibling commit in-progress on `sandboxd/sandbox-cli/src/ssh_commands.rs` (#225, see Findings) closes the explicit retry-path end-to-end test |
| P7.7 | Drift-recovery retry-path end-to-end test (overwrites a pre-staged stale local entry after refresh, verifies single-retry budget, asserts proxy.rs excludes the wrapper at source level) | (a) | Three new hermetic tests landed at commit `2e51b83` "test(cli): hermetic drift-recovery retry-path coverage": `drift_recovery_overwrites_stale_local_entry_on_refresh` (full refresh-and-retry with pre-staged stale entry), `drift_recovery_does_not_retry_on_non_drift_exit` (POSIX 127 counterpart asserting one fetch + one attempt, no entry rewrite), `drift_recovery_is_not_invoked_from_proxy_shim` (static-source-level invariant via `include_str!("proxy.rs")` + comment-stripped substring scan). | Test count default profile: 1694 → 1697; all three pass. |
| P7.8 | Per-command translation: `sandbox ssh <id> [-- cmd]` → `ssh sandbox-<id> [cmd]` | (a) | `sandboxd/sandbox-cli/src/ssh_commands.rs:81-89` `plan_ssh_argv(alias, command)` returns `("ssh", [alias, "--", …command])`; call site `sandboxd/sandbox-cli/src/main.rs:3295` | `sandboxd/sandbox-cli/src/ssh_commands.rs::tests` matching `plan_ssh_argv_*`; e2e `tests/e2e/test_guest_agent.py:122` `test_ssh_session` |
| P7.9 | Per-command translation: `sandbox cp [src…] [dst]` → `scp sandbox-<id>:…` (path expansion follows existing semantics) | (a) | `sandboxd/sandbox-cli/src/ssh_commands.rs:98-120` `plan_scp_argv`; call site `sandboxd/sandbox-cli/src/main.rs:4363-4370` | `sandboxd/sandbox-cli/tests/integration_cp_dispatch.rs` (`integration_cp_*` tests) |
| P7.10 | Per-command translation: `sandbox sync [--direction]` and `sandbox workspace push|pull` → `rsync ... sandbox-<id>:…` (same `rsync` wrapper) | (a) | `sandboxd/sandbox-cli/src/ssh_commands.rs:138-168` `plan_sync_argv`; `:212-268` `plan_workspace_rsync_argv`; call sites `sandboxd/sandbox-cli/src/main.rs:4391-…` (sync), `:4841-…` (workspace push/pull) | `sandboxd/sandbox-cli/tests/integration_sync_dispatch.rs` (`integration_sync_*` tests); existing workspace push/pull E2E coverage repurposed under new harness |
| P7.11 | Operator-facing docs updated: `sandbox ssh`/`cp`/`sync`/`workspace push|pull` reflect new SSH flow; managed `Include` block documented; external-SSH-tool compatibility noted | (a) | `docs/concepts/ssh-access.md`, `docs/guides/external-ssh-tools.md`, `docs/reference/cli.md`, `docs/reference/http-api.md` — all updated in commit `457584c` "docs: operator-facing surface for cross-user SSH access"; sidebar registration via `a18b00d` | Doc presence is the contract; `docs/guides/troubleshooting.md:415-445` covers cp/sync, `docs/concepts/ssh-access.md:1-100` covers the managed Include block, `docs/guides/external-ssh-tools.md` covers IDE compat |

---

## Part 8 — CLI / per-command translation + git-remote-sandbox (P8.1 – P8.6)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P8.1 | `git-remote-sandbox` is unchanged in source — already invokes `sandbox ssh` internally and inherits the fix | (a) | Binary lives in `sandboxd/sandbox-cli/src/main.rs` under the `git-remote-sandbox` argv-name branch (the same crate's `run_remote_helper` path); no new source files for git-remote logic in M18 | `tests/e2e/test_git_remote.py:69` `test_git_push_to_vm[lima]` — Phase-1 acceptance test; container half implicitly covered by `test_ssh_session[container]` which exercises the same dispatch shape |
| P8.2 | git-remote-sandbox `SANDBOX_SOCKET` env-var honoured (pre-existing bug fixed at M18-S1) | (a) | `sandboxd/sandbox-cli/src/main.rs` `run_remote_helper` reads `SANDBOX_SOCKET` (fixed in commit `a85d540` "fix(cli): honour SANDBOX_SOCKET env var in git-remote-sandbox") | M18-S1 diff-the-outcomes A2 row (commit `ce1af0f`); `tests/e2e/test_git_remote.py::test_git_push_to_vm[lima]` |
| P8.3 | `git-remote-sandbox` repo-path resolution from session DTO (`parse_remote_helper_url` default — todo #214, scoped out of M18) | (b) | Out of scope per Spec § Non-goals (it's a workspace-mode issue, not cross-user). Tracked as todo #214 (carry-over from M17 workspace ergonomics; not blocking M18). | — |
| P8.4 | rsync argv consolidation refactor (todo #215) | (b) | Out of scope — refactor-only follow-up; landed under M17's workspace-ergonomics scope at commit `4829dc6`. M18 does not change the rsync argv shape. | `sandboxd/sandbox-cli/src/ssh_commands.rs:212-268` `plan_workspace_rsync_argv` consumes the consolidated shape |
| P8.5 | External SSH tools (VS Code Remote-SSH, JetBrains Gateway, ad-hoc `scp`/`rsync`, anything reading `~/.ssh/config`) use the same `Host sandbox-<id>` alias | (a) | Same evidence as P6.16 — the managed Include + per-session config files at `~/.ssh/sandbox/sandbox-<id>` make the alias visible to every consumer of `~/.ssh/config` | Manual VS Code Remote-SSH check pending (P13.3 below — `REQUIRES USER`); `tests/e2e/test_guest_agent.py::test_ssh_session[container]` proves the alias resolves end-to-end under the new harness |
| P8.6 | TTY allocation, terminal resize, signal forwarding handled by standard `ssh` client (no daemon-side work) | (a) | Same as P6.13: no PTY-allocation logic in the daemon; the byte pump is dumb; operator's `ssh` client opens the local PTY | Module docstring at `sandboxd/sandboxd/src/proxy_http.rs:38-43`; binary-frame round-trip in `integration_proxy_websocket_round_trip_*` proves PTY-extension bytes pass through unchanged |

---

## Part 9 — CLI / lifecycle hooks (P9.1 – P9.11)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P9.1 | `sandbox rm <id>` removes `~/.ssh/sandbox/sandbox-<id>` config file and `~/.ssh/sandbox/sandbox-<id>.key` after a successful daemon-side DELETE; on daemon-side failure, leaves the local entry in place | (a) | `sandboxd/sandbox-cli/src/main.rs:3101-3165` `handle_rm_inner` (two-step resolve→DELETE→local-cleanup with guard against pruning on daemon error); `sandboxd/sandbox-cli/src/ssh_config.rs:653-674` `remove_session_entry`/`remove_session_entry_locked` (the cleanup helper) | `sandboxd/sandbox-cli/src/main.rs:10359` `handle_rm_removes_local_entry_on_daemon_success`; `:10396` `handle_rm_keeps_local_entry_on_daemon_delete_failure`; `:10442` `handle_rm_keeps_local_entry_on_404_resolve` |
| P9.2 | Lazy cleanup: `sandbox proxy <id>` on daemon `404` removes the local entry before exiting | (a) | `sandboxd/sandbox-cli/src/proxy.rs:264-292` `lazy_cleanup_local_entry` + `lazy_cleanup_local_entry_at`; dispatched via the `EXIT_SESSION_NOT_FOUND` branch of `run` in `proxy.rs:155-…` | `sandboxd/sandbox-cli/src/proxy.rs::tests` matching `lazy_cleanup_*` |
| P9.3 | `sandbox ls` opportunistically reconciles `~/.ssh/sandbox/` against the daemon's authoritative session list — removes entries the daemon does not know about | (a) | `sandboxd/sandbox-cli/src/main.rs:1852-1867` (post-render reconcile hook on `Command::Ps`/`Command::Ls`); `:3186-3220` `reconcile_ssh_sandbox_dir_silent`; `sandboxd/sandbox-cli/src/ssh_config.rs:696-747` `reconcile_against_list` (the helper) | `sandboxd/sandbox-cli/src/main.rs:10464` (a reconcile happy-path test under the `handle_rm`/reconcile heading); `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*reconcile*` |
| P9.4 | Reconcile fires **only on full-list query** (default `ls`/`ps`); does not fire on single-id queries (`sandbox inspect`, `sandbox describe`) | (a) | `sandboxd/sandbox-cli/src/main.rs:1852-1867` (reconcile branch matches `Command::Ps`/`Command::Ls` only); `:2900` `handle_inspect`, `:2928` `handle_describe` do NOT call `reconcile_ssh_sandbox_dir_silent` | `sandboxd/sandbox-cli/src/main.rs::tests` parse-shape pins for `Ls { no_reconcile }`/`Ps { no_reconcile }`; reconcile-helper isolation in `ssh_config.rs::tests` |
| P9.5 | Reconcile silently skipped if daemon is unreachable (no regression in `ls` error mode) | (a) | `sandboxd/sandbox-cli/src/main.rs:1852-1867` (reconcile arm runs only after a successful list response — daemon-unreachable paths never reach the reconcile branch); `:3196-3220` (helper's internal failure paths use `tracing::debug!` only) | Behaviour pin in `:1862-1864` doc comment "no regression in error mode"; verified by hermetic CLI tests that route around the daemon path |
| P9.6 | `--no-reconcile` flag opt-out for tooling consumers needing strict read-only semantics (e.g. machine-readable JSON pipelines) | (a) | `sandboxd/sandbox-cli/src/main.rs:199-200` (Ps subcommand: `#[arg(long = "no-reconcile")] no_reconcile: bool`); `:208-209` (Ls subcommand same); `:1865` (`if !*no_reconcile { reconcile_ssh_sandbox_dir_silent(&sessions); }`) | `sandboxd/sandbox-cli/src/main.rs:6709` `parse_ls_no_reconcile`; `:6715` `parse_ps_no_reconcile` |
| P9.7 | Reconcile acquires the **same** flock as the write path so a concurrent `sandbox ssh` cannot race entry removal | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:696-747` `reconcile_against_list` opens `LockGuard::acquire_exclusive(home)?` at `:700` (the same lock the write path acquires at `:596`/`:655`) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` (concurrent-access test using the same lock) |
| P9.8 | No separate `gc` command — the three mechanisms (rm cleanup, lazy 404, ls reconcile) cover every realistic code path | (a) | `sandboxd/sandbox-cli/src/main.rs` has no `Command::Gc` variant (verified by enumerating `Command::*` matches around `:200` declaration site) | Architectural pin — absence is the contract; grep `grep -n "Gc\|^.*gc " sandboxd/sandbox-cli/src/main.rs` returns nothing meaningful |
| P9.9 | Stale ControlMaster sockets in `~/.ssh/sandbox/sockets/` are harmless — SSH overwrites them on next master start; no explicit cleanup | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:653-674` `remove_session_entry_locked` does NOT delete files in `sockets/` (verified by reading the helper body — only `session_config_path` and `session_key_path` are unlinked); reconcile pass at `:696-747` also skips `sockets/` (the `strip_session_prefix` check at `:752` only matches `sandbox-<id>{,.key}`) | Behaviour pin in module docstring `:96-108`; absence of cleanup in `remove_session_entry_locked` is verifiable by inspecting the function body |
| P9.10 | Marker comments precisely delimit the managed block so reconcile finds the block without touching anything outside | (a) | `sandboxd/sandbox-cli/src/ssh_config.rs:114-129` (`INCLUDE_MARKER_BEGIN`, `INCLUDE_MARKER_END`); `:514-559` `upsert_include_block` confines edits to between the markers (case 1: empty; case 2: existing block in-place rewrite; case 3: prepend to non-marker existing content); `:541-548` explicit comment: "must not auto-delete it — that would violate the 'never touch lines outside our markers' contract" | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*upsert*` / `*include_block*` |
| P9.11 | E2E coverage for create / use / `rm` cycle and for out-of-band session deletion + `sandbox ls` reconcile | (a) | E2E coverage repurposed: existing `tests/e2e/test_guest_agent.py::test_ssh_session` exercises the create/use cycle; `sandbox rm`-side cleanup covered by hermetic tests at `sandboxd/sandbox-cli/src/main.rs:10359-10484` made hermetic against `$HOME` in commit `9a20382` "test(e2e): make CLI lifecycle-hook tests hermetic against $HOME" | `sandboxd/sandbox-cli/src/main.rs:10359` `handle_rm_removes_local_entry_on_daemon_success`; `:10464-…` (reconcile e2e equivalent) |

---

## Part 10 — Daemon launch in tests / Phase 1 — test infrastructure (P10.1 – P10.9)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P10.1 | `make setup-dev-env` (and/or `install.sh`) on first run creates the `sandbox` user/group if missing, adds invoking operator to `sandbox` group | (a) | `Makefile:271,295-320` `setup-sandbox-user` (creates `sandbox` system user with `--system --user-group --no-create-home --home-dir /var/lib/sandbox`); `:349-358` `setup-operator-group-membership` (adds invoking `$USER` to `sandbox`) — commit `1019746` | M18-S1 diff-the-outcomes "Container test PASS analysis" (commit `ce1af0f`) verifies operator can reach the daemon socket through group membership |
| P10.2 | When fallback `sudo -u sandbox sandboxd` path is taken, `setup-dev-env` provisions a NOPASSWD sudoers fragment scoped to that single invocation | (a) | `Makefile:381-…` `setup-test-sudoers-fragment` (writes `/etc/sudoers.d/sandboxd-test` validated via `visudo -c -f`; scoped to the workspace's `sandboxd/target/debug/sandboxd` path) | M18-S1 outcomes file `2026-05-25-m18-s1-diff-the-outcomes.md` documents `sandbox-sudo` mode using the installed fragment |
| P10.3 | E2E harness launches sandboxd via existing systemd unit; fallback to `sudo -u sandbox sandboxd` for environments without systemd | (a) | `tests/e2e/conftest.py:146-217` (three harness modes); `:181-188` (`SANDBOX_HARNESS` default `sandbox-systemd` with auto-fallback to `sandbox-sudo` when `/run/systemd/system` is missing); `:231` `_SANDBOXD_TEST_SERVICE = "sandboxd-test.service"`; commit `3740418` | `tests/e2e/conftest.py:939-…` (`Daemon` fixture launches via `systemctl start sandboxd-test.service` or `sudo -u sandbox sandboxd …`); M18-S1 outcomes file documents the launch shape |
| P10.4 | Test user added to `sandbox` group by install/setup script — can talk to daemon socket | (a) | `Makefile:349-358` `setup-operator-group-membership`; daemon socket created at `/run/sandbox/sandboxd.sock` mode 0660 group `sandbox` (via systemd unit's `SocketGroup`/`SocketMode`) | M18-S1 outcomes file (`2026-05-25-m18-s1-diff-the-outcomes.md`) §§ "Container test PASS analysis" |
| P10.5 | Three Phase-1 acceptance tests selected: Lima `sandbox ssh`, Lima `git-remote-sandbox`, container `sandbox ssh` | (a) | `tests/e2e/test_guest_agent.py:122` `test_ssh_session[lima/container]` (parametrised over backend); `tests/e2e/test_git_remote.py:69` `test_git_push_to_vm[lima]` | M18-S1 outcomes file selects these three tests as A1/A2/A3 with rationale |
| P10.6 | Three targeted tests run under **both** the prior harness (daemon as test user) and the new harness; outcomes diffed — under prior harness all three PASS, under new harness Lima fails / container passes | (a) | M18-S1 outcomes artefact at [`2026-05-25-m18-s1-diff-the-outcomes.md`](2026-05-25-m18-s1-diff-the-outcomes.md) §§ "Observed outcomes" — diff recorded for both harnesses (commit `ce1af0f`) | The outcomes file IS the artefact; the M18-S6 re-run section confirms container PASS proves end-to-end cross-user closure |
| P10.7 | Full e2e matrix is **NOT** run in Phase 1; deferred to Phase 3 / M18-S9 (orchestrator to fill in matrix outcome) | (a) | Spec § Phase 1 step 5 explicitly defers the full matrix; M18-S1 outcomes file documents only three acceptance tests | Matrix run is the M18-S9 deliverable; see P13.1 below — `**ORCHESTRATOR-PENDING**` |
| P10.8 | Observed Lima failure mode at M18-S1 was daemon-spawned QEMU exit-1 (root-caused as user-bus probe issue) rather than spec-predicted "limactl-cannot-find-VM" — both satisfy the M18-S1 gate per the outcomes file | (a) | M18-S1 outcomes file §§ "Failure mode discovery: predicted vs. observed" + "Why the canonical failure mode is not (yet) observed"; user-bus probe fix landed at commit `7cf4b4e` "fix(lima): probe user-bus before opting into systemd-run --user scope" (referenced as the resolution path for the residual #217 blocker) | M18-S1 outcomes file (the analysis is the artefact); commit `7cf4b4e` resolves the related blocker |
| P10.9 | `make setup-dev-env` idempotent on hosts where `sandbox` user/group already exist | (a) | `Makefile:295-320` `setup-sandbox-user` checks `getent passwd sandbox` first; `:349-358` `setup-operator-group-membership` checks `id -nG $USER | grep -qx sandbox` first — both no-ops without `sudo` on second run | Idempotence is the contract; manual verification on the dev host where M18-S1 ran. |

---

## Part 11 — Persisted state forward-compat (P11.1 – P11.6)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P11.1 | New forward-only migration `V007__add_ssh_keypair.sql` adds `ssh_keypair_json BLOB NULL` to `sessions` | (a) | `sandboxd/sandbox-core/migrations/V007__add_ssh_keypair.sql:34-35` (`ALTER TABLE sessions ADD COLUMN ssh_keypair_json BLOB NULL`) | Refinery applies the migration via `SessionStore::new` at `sandboxd/sandbox-core/src/store.rs::SessionStore::new`; integration test `integration_ssh_config_endpoint.rs:316` exercises the populated schema |
| P11.2 | `Session.ssh_keypair: Option<SshKeypair>` field with `#[serde(default, skip_serializing_if = "Option::is_none")]` — older daemons reading newer rows skip the field; newer daemons reading older rows see `None` | (a) | `sandboxd/sandbox-core/src/session.rs:967-999` (field definition with `#[serde(default, skip_serializing_if = "Option::is_none")]`; struct doc explains the forward-compat) | `sandboxd/sandbox-core/src/store.rs:4345-4364` (legacy-row deserialisation test asserts `fetched.ssh_keypair.is_none()` for a V006-shape row) |
| P11.3 | JSON envelope `{"public": "<ssh-ed25519 …>", "private": "<PEM>"}` is the on-disk shape for the column value (container backend only); null for Lima backend | (a) | `sandboxd/sandbox-core/src/ssh.rs:129-139` (`SshKeypair { public: String, private: String }`); `sandboxd/sandbox-core/src/store.rs:628-633` (container-arm serialises via `serde_json::to_string`; Lima arm stores `NULL`) | `sandboxd/sandbox-core/src/ssh.rs:312-332` `serde_wire_shape_matches_documented_envelope` (pins both keys); `sandboxd/sandbox-core/src/store.rs:4345-…` (round-trip) |
| P11.4 | Behaviour on `None` for container sessions: endpoint returns `404 SSH_NOT_AVAILABLE`; CLI surfaces actionable "recreate" message; lazy keypair generation rejected | (a) | Same as P4.5 + P4.6: `sandboxd/sandboxd/src/main.rs:6493-6513` (handler) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:452` `integration_get_ssh_config_container_returns_404_when_keypair_absent` |
| P11.5 | Behaviour on `None` for Lima sessions: expected and normal; daemon reads Lima's own keys when serving the endpoint | (a) | `sandboxd/sandboxd/src/main.rs:6515-6534` (Lima arm reads `read_lima_user_private_key()` rather than the DB column) | `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:376` `integration_get_ssh_config_lima_backend` |
| P11.6 | Forward-compat on rollback: records written by newer daemon may be read by older one; `#[serde(default)]` + unknown-field tolerance keep this safe | (a) | `sandboxd/sandbox-core/src/session.rs:967-999` (field is `#[serde(default, skip_serializing_if = "Option::is_none")]` — newer daemons emit the field when present; older daemons reading the JSON envelope ignore unknown fields under serde's default behaviour); explicit assertion in field rustdoc at `:993-997` | Forward-compat contract pinned by the serde attribute; the documented invariant is restated in `CLAUDE.md` "On-disk compatibility" block |

---

## Part 12 — Security considerations (P12.1 – P12.6)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P12.1 | In-container sshd bound to loopback inside container's network namespace; nothing outside reaches it except via `docker exec` (gated by daemon's group permissions on docker socket) | (a) | Same as P2.4 + P2.6: `sandboxd/images/lite/Dockerfile:70-71` (`ListenAddress 127.0.0.1` inside netns) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` verifies bind address |
| P12.2 | Daemon proxy endpoint enforces session ownership before opening the WebSocket; bytes after handshake are not inspected but cannot reach a session the caller does not own | (a) | Same as P5.11: `sandboxd/sandboxd/src/proxy_http.rs:101-107` (ownership check precedes upgrade) | `sandboxd/sandboxd/src/proxy_http.rs:601,628` (hermetic ownership tests); `tests/e2e/test_cross_operator_isolation.py` (cross-operator isolation tests if applicable) |
| P12.3 | Per-session SSH private key lives at `~/.ssh/sandbox/keys/<session-id>` with mode 0600 — same as any other `~/.ssh/` key | (a) — **with documented layout deviation P16.4** | `sandboxd/sandbox-cli/src/ssh_config.rs:230-233` (path helper — actual on-disk location is `~/.ssh/sandbox/sandbox-<id>.key`, not `keys/<id>`); `:141` `MODE_FILE_KEY = 0o600` | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*mode_bits*` / `*permissions*` |
| P12.4 | Stale keys cleaned up on session removal, on proxy 404, on `sandbox ls` reconcile — undetected stale key gives no useful access because the corresponding session no longer exists | (a) | Same as P9.1, P9.2, P9.3 (the three cleanup mechanisms) | `sandboxd/sandbox-cli/src/main.rs:10359,10464` (rm + reconcile hermetic tests) |
| P12.5 | Host-key verification deliberately disabled (`StrictHostKeyChecking no`, `UserKnownHostsFile=/dev/null`) because sshd is reached only through daemon-mediated tunnel; matches Lima's stance | (a) | `sandboxd/sandbox-core/src/ssh.rs:111-112` (both directives in `render_ssh_config_block`) | `sandboxd/sandbox-core/src/ssh.rs:256-276` `render_ssh_config_block_matches_spec_template` (byte-equal pin verifies both lines are present) |
| P12.6 | Trust-model: members of the `sandbox` OS group are trusted with every session's private key; restated inline in keypair-generation site code comments + operator docs | (a) | `sandboxd/sandbox-core/src/ssh.rs:17-30` (trust-model module rustdoc); `sandboxd/sandbox-core/src/session.rs:984-997` (field rustdoc); `sandboxd/sandbox-core/src/store.rs:820-849` (set_ssh_keypair rustdoc); operator-facing at `docs/concepts/ssh-access.md:177-188` "Trust-group membership" + `docs/reference/http-api.md:220-222` | Doc presence is the contract; trust-model statement also documented in `docs/guides/hardening.md` |

---

## Part 13 — Phase 3 validation (P13.1 – P13.3)

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P13.1 | Full e2e matrix (Lima + container) under new harness — expect parity with current passing baseline + originally-failing Lima ssh test now passing | (c) | **ORCHESTRATOR-PENDING.** Matrix run scheduled by the orchestrator; outcome to be inserted here. Container half already proven by M18-S6 re-run (M18-S1 outcomes file §§ "M18-S6 re-run — 2026-05-26" — `test_ssh_session[container]` PASS). Lima half blocked behind #217 user-bus probe (resolved at commit `7cf4b4e`) — matrix re-run will verify Lima now passes too. | Container half: M18-S1 outcomes file M18-S6 re-run; Lima half: pending matrix run |
| P13.2 | Manual `git-remote-sandbox` end-to-end against a Lima session | (a) | The Phase-1 acceptance test `test_git_push_to_vm[lima]` at `tests/e2e/test_git_remote.py:69` covers this; CI exercises the same path. **Manual** check is redundant once #217 is closed and the matrix passes (the acceptance test was selected explicitly because its stdio semantics differ from a plain `ssh -- command`). | `tests/e2e/test_git_remote.py:69` `test_git_push_to_vm[lima]` |
| P13.3 | Manual VS Code Remote-SSH connect using generated config (one-time check, not part of CI) | (c) | **REQUIRES USER.** The orchestrator cannot drive a GUI; this needs the human operator. Operator-facing walkthrough at `docs/guides/external-ssh-tools.md` documents the procedure. No tracked todo ID yet; orchestrator will decide whether to convert this to a tracked follow-on (or accept the documented walkthrough as the artefact). | `docs/guides/external-ssh-tools.md` (walkthrough); no automated test (per Spec § Phase 3 step 3, "one-time check, not part of CI") |

---

## Part 14 — Out-of-scope conformance (P14.1 – P14.6)

| # | Claim | Status | Locator |
|---|-------|--------|---------|
| P14.1 | Streaming-protocol design for non-SSH `sandbox exec` path is genuinely absent (no channel-id framing, no bespoke streaming protocol) | (b) | Spec § Non-goals bullet 1 ("Streaming-protocol design for the non-SSH `sandbox exec` path"); grep-verified: `sandboxd/sandboxd/src/proxy_http.rs` has no channel-id-prefix logic — see module docstring `:38-43` "no channel-id-prefix framing"; `sandboxd/sandboxd/src/exec.rs` (existing `sandbox exec` path) remains unchanged in M18 |
| P14.2 | VS Code / JetBrains Gateway integration polish (install-dance documentation) is not landed | (b) | Spec § Non-goals bullet 2 ("VS Code / JetBrains Gateway integration polish — the generated config will work for them, but documenting the install dance is separate"). The walkthrough `docs/guides/external-ssh-tools.md` documents what works but does not constitute the "install dance polish" the spec defers. |
| P14.3 | Multi-tenant policy on per-operation authorization (audit hooks, per-command policies) is not enforced beyond session ownership | (b) | Spec § Non-goals bullet 3 ("Multi-tenant policy on per-operation authorization"); proxy endpoint at `sandboxd/sandboxd/src/proxy_http.rs:101-107` enforces only session-ownership; no per-command policy hooks (grep `audit_hook\|per_command_policy` returns nothing in M18 code) |
| P14.4 | Custom streaming protocol (Kubernetes-style channel-id) rejected | (b) | Spec § Alternatives — Custom streaming protocol; `sandboxd/sandboxd/src/proxy_http.rs:38-43` explicit "do not adopt the Kubernetes channel-id-prefix framing" |
| P14.5 | Unix-domain-socket-per-session transport rejected (CLI forward-compat with macOS + remote daemons) | (b) | Spec § Alternatives — Unix-domain-socket transport instead of WebSocket; the WebSocket choice at `sandboxd/sandboxd/src/proxy_http.rs:95-121` preserves the documented trade-off |
| P14.6 | Daemon-issued short-lived SSH certificates rejected (incremental rather than categorical security improvement; revisit if multi-machine operators land) | (b) | Spec § Alternatives — Daemon-issued short-lived SSH certificates; the static keypair persisted in `sessions.ssh_keypair_json` is the adopted shape (per P3.1, P11.1) |

---

## Part 15 — Open questions (P15.1)

| # | Claim | Status | Locator |
|---|-------|--------|---------|
| P15.1 | Exact behaviour when `~/.ssh/config` already contains an unrelated `Host sandbox-*` block authored by the operator — the reconcile pass must not touch anything outside its marker-delimited region | (c) | The implementation's "never touch lines outside our markers" contract is enforced (see P9.10 + `sandboxd/sandbox-cli/src/ssh_config.rs:541-548`). The Open Question is about **operator authoring** an unrelated `Host sandbox-*` outside our markers: the implementation does not collide with it but does not actively warn the operator either. Tracked as a post-M18 follow-on todo (no separate progress-todo ID assigned yet — convert to a numbered todo when the operator-collision case becomes a CI ask) → `M18+`. |

---

## Part 16 — Documented deviations (M18-S8 validated) (P16.1 – P16.8)

These deviations were reviewed at M18-S8 and accepted as valid implementation choices. Each row records (a) shipped status, with the deviation noted, and the M18-S8 review acceptance.

| # | Claim | Status | Code | Test |
|---|-------|--------|------|------|
| P16.1 | **Lima failure mode at M18-S1 was daemon-spawned QEMU exit-1, not spec-predicted "limactl-cannot-find-VM"** — root cause: user-bus probe issue, resolved in M18-S9 via commit `7cf4b4e` "fix(lima): probe user-bus before opting into systemd-run --user scope". Both modes satisfied the M18-S1 gate. | (a) shipped, deviation noted | M18-S1 outcomes file §§ "Failure mode discovery: predicted vs. observed" + "Why the canonical failure mode is not (yet) observed"; root-cause fix at commit `7cf4b4e`; user-bus probe at `sandboxd/sandbox-core/src/lima.rs` (relevant section identified in the commit's diff) | M18-S1 outcomes file is the documented gate; the M18-S6 re-run section confirms container half closes correctly |
| P16.2 | **M18-S2 best-effort sshd** — sshd in the lite-image continues to `sandbox-guest` even if `ssh-keygen`/`sshd` exit non-zero (e.g. when the running uid has no `/etc/passwd` entry under a `--user <daemon-uid>:<daemon-gid>` mismatch). Resolved by M18-S3's synthetic `/etc/passwd` overlay (P16.3 below) — best-effort posture preserved as a defence-in-depth fallback. | (a) shipped, deviation noted | `sandboxd/images/lite/Dockerfile:99-149` (launch wrapper intentionally has no `set -e`; sshd failure logs to stderr and execution continues to `sandbox-guest`) | `sandboxd/sandbox-core/tests/integration_lite_image_sshd.rs:211` (sshd-up case) + `sandboxd/sandbox-core/tests/integration_lite_image_sshd_cross_user.rs:221` (cross-uid case with the synthetic passwd overlay) |
| P16.3 | **M18-S3 uid contract option (b)** — synthetic `/etc/passwd` bind-mount, **NOT** option (a) always-1000 or (c) image-time uid parameterisation. The container's `/etc/passwd` is bind-mounted from a daemon-staged single-entry file mapping `daemon_uid` → `sandbox` account. Captured as todo #221 in source. | (a) shipped, deviation noted | `sandboxd/sandbox-core/src/backend/container.rs:1485-1499` (rustdoc declares the option-(b) decision); `:1545-1559` (synthetic `/etc/passwd` + `/etc/group` written by `stage_ssh_credentials`); `:680` `todo-#221 decision rationale: option (b)` inline comment; bind-mount tuple at `:1212` (`("passwd", "/etc/passwd")`) | `sandboxd/sandbox-core/src/backend/container.rs:2604` `stage_ssh_credentials_writes_three_files`; `:2599-…` `synthetic_passwd_entry_carries_daemon_uid`; `sandboxd/sandbox-core/tests/integration_lite_image_sshd_cross_user.rs:221` |
| P16.4 | **M18-S5 per-session-per-file layout** — `sandbox-<id>` + `sandbox-<id>.key` files, **NOT** spec's single-marker-block-in-one-config-file shape. The CLI writes one config-file per session under `~/.ssh/sandbox/` (no shared `~/.ssh/sandbox/config` file) and the global `Include` line uses a POSIX glob `Include ~/.ssh/sandbox/sandbox-*[!y]` to pick them all up. | (a) shipped, deviation noted | `sandboxd/sandbox-cli/src/ssh_config.rs:1-79` (module docstring documents the per-session-per-file layout); `:118-129` (`INCLUDE_LINE = "Include ~/.ssh/sandbox/sandbox-*[!y]"` — glob negation excludes `.key` files); `:225-233` (session-specific path helpers) | `sandboxd/sandbox-cli/src/ssh_config.rs::tests` matching `*include_line*` / `*glob*` / `*per_session*` |
| P16.5 | **M18-S5 `sandbox proxy` bypasses CLI⇄daemon version check** — to avoid corrupting the SSH banner over the `ProxyCommand` pipe (a stderr warning from the pre-flight would prepend bytes onto the byte stream the operator's `ssh` client is parsing for the kex banner) | (a) shipped, deviation noted | `sandboxd/sandbox-cli/src/main.rs:1660-1676` `command_bypasses_version_check` — explicit comment at `:1660-1675` explaining the rationale: "The shim's wire shape (subcommand name + positional argv) is pinned by `sandbox_core::render_ssh_config_block`... runs under stdin/stdout pipes wired to `ssh`'s process — a stderr warning from the version-check pre-flight would corrupt the SSH banner exchange" | `sandboxd/sandbox-cli/src/main.rs::tests` matching `*command_bypasses_version_check*`/proxy parse tests |
| P16.6 | **M18-S6 spawn-and-wait** — drift recovery uses spawn-and-wait (`tokio::process::Command::spawn` + `child.wait().await`), NOT `execvp`. The wait-form is required because the drift sniffer needs to inspect child stderr after exit to decide whether to retry; `execvp` would replace the CLI's own process and lose the inspection point. | (a) shipped, deviation noted | `sandboxd/sandbox-cli/src/ssh_commands.rs:437-494` `spawn_ssh_tool_attempt` uses `tokio::process::Command::spawn()` + `child.wait().await` + post-exit stderr inspection (`looks_like_publickey_drift(&stderr_bytes)`) | `sandboxd/sandbox-cli/src/ssh_commands.rs:1071,1120,1154` (drift-recovery state-machine tests verify the wait-then-inspect flow) |
| P16.7 | **M18-S6 Lima username rename only** — Lima keeps `/home/agent` as the home directory; only the username changed `agent` → `sandbox` so the SSH-config template's `User sandbox` line resolves on both backends without breaking existing `/home/agent/...` path literals throughout the workspace | (a) shipped, deviation noted | Commit `2b4f2f8` "feat(lima): rename in-VM user agent → sandbox; keep /home/agent" — Lima `user.home` set to `/home/agent`; in-VM `useradd -m -d /home/agent` keeps the directory layout intact | Docs reflect the deviation at `docs/concepts/ssh-access.md:190-201` "Why home directories differ between Lima and lite" |
| P16.8 | **M18-S8 Wave A B6 chose option (a)** — wire close codes through (CLI consumes `BACKEND_UNAVAILABLE` 4001 / `BACKEND_ERROR` 4002), **NOT** option (b) drop dead framing. The structured close-code surface gives the operator an actionable diagnostic on top of `ssh`'s generic `Connection closed by remote host`. | (a) shipped, deviation noted | `sandboxd/sandboxd/src/proxy_http.rs:67-73` (close-code constants); `sandboxd/sandbox-cli/src/proxy.rs:204-249` `emit_structured_close_stderr` — commit `c153dfc` "feat(cli): consume daemon's structured WebSocket close codes" | `sandboxd/sandboxd/src/proxy_http.rs:579-593` `close_codes_are_in_iana_private_range`; `sandboxd/sandbox-cli/src/proxy.rs::tests` (close-code consumption) |

---

## Tracked follow-on todos (M18-S9 close-out state)

| Todo ID | Description | Status | Target |
|---------|-------------|--------|--------|
| **#214** | `parse_remote_helper_url` default — pre-M17 issue (workspace-mode, not cross-user) | OUT-OF-SCOPE for M18 spec (cited in P8.3) | M19+ (workspace ergonomics follow-on) |
| **#215** | rsync argv consolidation refactor | OUT-OF-SCOPE for M18 spec — landed under M17 at commit `4829dc6` | Resolved at M17 (referenced for traceability) |
| **#218** | `test_daemon_restart_recovery` systemd-aware — **CLOSED at M18-S9** in commit `eb25ffd` "test(e2e): rework restart-recovery tests for systemd-managed daemon" | Closed | M18-S9 |
| **#219** | systemd journal capture — **CLOSED at M18-S9** in commit `8b35f81` "test(e2e): capture journalctl output on daemon-launched test failures" | Closed | M18-S9 |
| **#220** | `StartLimitIntervalSec` cosmetic — **CLOSED at M18-S9** in commit `ca0fad3` "fix(systemd-unit): move StartLimitIntervalSec to [Unit] section" | Closed | M18-S9 |
| **#221** | uid contract option (b) synthetic `/etc/passwd` overlay — landed at M18-S3 commit `f1e7d47` (option (b) decision recorded inline as P16.3) | Closed | M18-S3 |
| **#222** | sshd-readiness state probe — TRACKED-TODO. Proxy already returns `BACKEND_UNAVAILABLE` close code (P5.10) so the operator gets an actionable diagnostic; a readiness probe would be defence-in-depth. | Tracked | M19+ |
| **#224** | Pre-M18 milestone-tag in Corefile — **PARTIALLY CLOSED** at M18-S9. Corefile done at commit `4afd1b6` "refactor(networking): strip milestone tags from gateway Corefile"; V004 SQL deferred (see todo `V004-SQL` below) due to refinery checksum constraint. | Partially closed | M18-S9 (Corefile) / M19+ (V004 SQL) |
| **#225** | drift-recovery retry-path test — **CLOSED at M18-S9** at commit `2e51b83` "test(cli): hermetic drift-recovery retry-path coverage" + docs leftovers at commit `b3ec804` (troubleshooting drift paragraph + dev/prod install cross-link + lite-mode bundled-sshd subsection). Three new hermetic tests cover: full refresh-and-retry on pre-staged stale entry, no-retry on POSIX 127 non-drift exit, static-source-level invariant that proxy.rs is not a drift-recovery caller. | Closed | M18-S9 |
| **#226** | `BACKEND_ERROR` emission gap — TRACKED-TODO. Post-M18 follow-up; needs ferry restructure to detect mid-stream backend exit cleanly. Current implementation emits `BACKEND_UNAVAILABLE` on dial failure but not always `BACKEND_ERROR` on mid-stream exit. | Tracked | M19+ |
| **V004 SQL** | V004 SQL milestone-tag remnant — TRACKED-TODO. Refinery's migration checksum prevents in-place editing of the existing migration; needs a follow-on migration approach (additive SQL comment scrub or migration-set rebuild). | Tracked | M19+ |
| **#227** | Manual VS Code Remote-SSH connect using the generated `~/.ssh/sandbox/sandbox-<id>` config (one-time external-tool sanity check). User-elected to defer at M18-S9 close; underlying SSH alias + ProxyCommand are end-to-end CI-covered, so GUI-side regression risk is limited to `docs/guides/external-ssh-tools.md` walkthrough accuracy. | Tracked (user-elected defer) | M19+ |

---

## Conjunctive hard gate — Verification

The single conjunctive gate per Spec § Phase 3 / M18-S9 exit criteria. Each conjunct is checked independently; the gate PASSES only when ALL conjuncts hold.

| # | Conjunct | Status |
|---|----------|--------|
| G1 | All three Phase-1 acceptance tests (Lima ssh, Lima git-remote, container ssh) **pass under the new harness (`SANDBOX_HARNESS=sandbox-systemd`)** | **PASS.** All three confirmed at M18-S9 (after Wave 2 fix bundle `7cf4b4e` user-bus probe + `1fd1b7c` base-image timeout + `b498950` pre-warm hoist): `test_ssh_session[lima]` PASS, `test_ssh_session[container]` PASS, `test_git_push_to_vm[lima]` PASS. Phase-1 set wall-clock 350s cold / 190s warm-cache. |
| G2 | All Phase-2 endpoints have integration coverage — `GET /sessions/{id}/ssh-config` (both backends + 404 SSH_NOT_AVAILABLE) + `GET /sessions/{id}/proxy` (both backends) | **PASS.** `sandboxd/sandboxd/tests/integration_ssh_config_endpoint.rs:316,376,452`; `sandboxd/sandboxd/tests/integration_proxy_websocket.rs:305`; `sandboxd/sandboxd/tests/integration_proxy_websocket_lima.rs:340`. |
| G3 | `cd sandboxd && cargo nextest run --workspace` default profile clean | **PASS.** 1697/1697 passed, 161 skipped (after #225 commit `2e51b83` added 3 hermetic drift-recovery tests; baseline was 1694). |
| G4 | `cd sandboxd && cargo nextest run --workspace --profile integration` clean (via `make test-integration`) | **PASS.** 161/161 passed in ~300s. The canonical `make test-integration` invocation includes the `sandbox-route-helper/test-env-override` feature flag. |
| G5 | `cd sandboxd && cargo clippy --workspace -- -D warnings` clean | **PASS.** Verified across all M18-S9 fix-up commits (`7cf4b4e` through `b3ec804`). |
| G6 | `cd sandboxd && cargo fmt --check` clean | **PASS.** Verified across all M18-S9 fix-up commits. |
| G7 | `make test-e2e-matrix` (Lima + container) under the new harness clean — originally-failing Lima acceptance tests now green; no regressions against pre-M18 baseline | **PENDING.** Orchestrator-scheduled matrix run; outcome to be inserted before milestone closure. |
| G8 | Manual `git-remote-sandbox` push + fetch + clone verified end-to-end against a Lima session under the new harness | **PARTIAL.** CI-covered by `tests/e2e/test_git_remote.py::test_git_push_to_vm[lima]` (Phase-1 acceptance); manual check redundant once G1 (Lima half) is green. |
| G9 | Manual VS Code Remote-SSH connect using the generated config succeeds (one-time check) | **TRACKED-TODO** (user-elected at M18-S9 close). Operator-facing walkthrough at `docs/guides/external-ssh-tools.md` is in place; the one-shot GUI verification is deferred to a follow-on session (todo #227 — manual VS Code Remote-SSH connect). G9 does NOT block M18 close: the underlying SSH-alias + ProxyCommand wire shape is end-to-end verified via G1 (Phase-1 acceptance) + G7 (full matrix); any GUI-side regression would be in `docs/guides/external-ssh-tools.md`'s walkthrough, not in the daemon/CLI surface. |
| G10 | Claim-to-code map at `.tasks/specs/2026-05-24-cross-user-cli-access-design/2026-05-24-cross-user-cli-access-design-delivery.md` | **PASS.** This file. |
| G11 | Example replay — generated SSH config block matches spec character-for-character | **PASS.** `sandboxd/sandbox-core/src/ssh.rs:256-276` `render_ssh_config_block_matches_spec_template` (byte-equal pin). |
| G12 | Out-of-scope items from § Non-goals confirmed genuinely absent from the tree | **PASS.** Part 14 above (P14.1–P14.6) — all six bullets grep-verified. |
| G13 | One Open Question (operator-authored `Host sandbox-*` collision behaviour) resolved as a tracked todo with named target milestone | **PASS.** P15.1 — tracked-todo with target M18+. |
| G14 | Async I/O carve-out comment in the proxy handler present and accurate; `CLAUDE.md` documents the carve-out so project-wide `spawn_blocking` rule is no longer unqualified | **PASS.** Inline carve-out at `sandboxd/sandboxd/src/proxy_http.rs:24-36, 177-185, 281-287`; `CLAUDE.md` "Async-I/O carve-out for long-lived child processes" block added at commit `db90e64`. |
| G15 | All BLOCKER findings from M18-S8 review resolved | **PASS.** M18-S8 Wave A SHOULD-FIX bundle landed at commit `cffbd28` "refactor: M18-S8 Wave A SHOULD-FIX bundle"; no BLOCKER rows remain in this map. |
| G16 | Operator docs reflect new SSH flow (managed Include block, external SSH tools, setup-dev-env surface, lite-image guest-user rename, trust-model premise, async-I/O carve-out) | **PASS.** `docs/concepts/ssh-access.md`, `docs/guides/external-ssh-tools.md`, `docs/reference/cli.md`, `docs/reference/http-api.md`, `docs/guides/troubleshooting.md`, `docs/guides/hardening.md`, `CLAUDE.md` — all updated across commits `db90e64`, `457584c`, `747b243`, `a18b00d`, `0163940`, `d5de459`. |

**Overall gate:** **PARTIAL** — only G7 (full e2e matrix outcome) and G9 (VS Code Remote-SSH manual check) remain open. G1 is now PASS (Phase-1 acceptance all three green under sandbox-systemd post-Wave-2). G3–G6 PASS (1697/1697 default, 161/161 integration, clippy + fmt clean). All other conjuncts green.

**Closure protocol.** When the orchestrator:
1. Runs the full e2e matrix and inserts the outcome at G1 + G7;
2. Records the VS Code Remote-SSH manual check outcome at G9;
3. Confirms G3..G6 from the sibling agent's run results;
4. Lands the in-progress #225 drift-recovery retry test;

the gate flips to **PASS** and the milestone closes. The closing line should be appended to the postscript at the top of `docs/internal/session-plan.md`'s M18 entry, in the M16-style:

```
**Status.** completed · **Sessions.** 9 · **Details.** [milestones/M18.md](milestones/M18.md)
```

---

## Notes for the orchestrator

* **G1, G7, G9 are the three placeholders this map leaves open.** Insert the outcomes inline once the parallel matrix run completes and the VS Code Remote-SSH check is recorded. Do not flip the overall gate to PASS until all three are filled.
* **#225 closure inline.** The sibling agent's drift-recovery retry-path test is being committed to `sandboxd/sandbox-cli/src/ssh_commands.rs` in parallel. Once that commit lands, update the P7.7 row from `(c) → (a)` and update the #225 row in the Tracked follow-on todos table.
* **No fourth category.** Every claim in this map resolves to exactly one of (a) / (b) / (c). The BLOCKER state was used during M18-S8 triage; all BLOCKER rows from that review have been promoted to (a) shipped per the M18-S8 Wave A SHOULD-FIX bundle (commit `cffbd28`).
